# 2026-07-28

## 작업 일시

- 2026-07-28 19:12 KST

## 작업 요약

- Lucide 런타임 의존성, 소스 import, 번들 코드, Playground SVG를 제거했다.
- 공개 정렬 표시와 필요한 UI glyph를 module-owned CSS 또는 접근성 안전한 텍스트로 대체했다.
- 현재 package artifact의 외부 번들 source 재유입을 차단하는 gate를 추가했다.
- README demo GIF를 변경 후 Playground에서 재생성했다.
- 이 보고 단계에서는 제품 코드, package 파일, 버전, 원격 상태를 변경하지 않았다.

## 변경 파일

- 라이브러리 구현 및 스타일: `src/index.tsx`, `styles.css`
- Playground 구현 및 스타일: `example/src/components/FeatureControls.tsx`, `example/src/components/docs/DocsTopNav.tsx`, `example/src/components/ui/multi-select.tsx`, `example/src/features/BasicCrudFeature.tsx`, `example/src/features/ColumnGroupFeature.tsx`, `example/src/features/CoreFeature.tsx`, `example/src/features/HeaderFeature.tsx`, `example/src/features/InfiniteScrollFeature.tsx`, `example/src/features/LazyLoadFeature.tsx`, `example/src/features/PaginationFeature.tsx`, `example/src/styles.css`
- package manifest, lockfile, scaffold: `package.json`, `package-lock.json`, `components.json`
- package artifact gate 및 회귀 테스트: `scripts/verify-package-artifact.mjs`, `test/package-artifact-gate.node.mjs`, `test/public-api-boundary.test.ts`, `test/repository-hygiene.test.ts`, `test/table-interaction.test.tsx`
- Playground 회귀 테스트: `test/playwright/specs/header-basic.spec.ts`, `test/playwright/specs/playground-layout-polish.spec.ts`
- 공개 문서와 legacy notice: `docs/user/06-header.md`, `docs/ko/06-header.md`, `THIRD_PARTY_NOTICES.md`
- README demo artifact: `docs/assets/comins-table-demo.gif`
- 작업 보고: `reports/2026-07-28.md`

## 검증

### 현재 active surface

Command:

```bash
rg -n 'lucide-react|className="lucide|//#region node_modules/lucide' src example dist package.json package-lock.json components.json docs/user docs/ko README.md
```

Result: 출력 없음, exit 1. `rg`의 no-match 종료 코드이며 의도적인 legacy notice, 계획, 과거 report는 검사 범위에서 제외했다.

Command:

```bash
npm ls lucide-react --all
```

Result: dependency tree는 `(empty)`, exit 1. npm의 absent-package 종료 의미를 그대로 기록한다.

### 라이브러리 baseline

Command:

```bash
npm run verify
```

Result: exit 0.

- repository hygiene PASS
- security Node tests 12/12 PASS
- TypeScript no-emit lint PASS
- Vitest 16 files, 154/154 tests PASS
- Vite build 및 declaration build PASS (`9 modules transformed`)

### 영향받은 Playground E2E

Command:

```bash
npm run test:e2e -- \
  test/playwright/specs/component-renderer.spec.ts \
  test/playwright/specs/header-quality.spec.ts \
  test/playwright/specs/header-basic.spec.ts \
  test/playwright/specs/playground-layout-polish.spec.ts \
  test/playwright/specs/basic-playground.spec.ts \
  test/playwright/specs/readme-demo.spec.ts \
  --workers=1
```

Result: exit 0, 52/52 PASS.

### 전체 non-perf Playground E2E

Command:

```bash
npm run test:e2e -- --workers=1
```

Result: exit 0, 88/88 PASS. 로컬 server bind 실패 또는 재시도는 발생하지 않았다.

성능 suite는 실행하지 않았다. 이번 변경은 virtualization, scrolling, memory counter 또는 performance test contract를 변경하지 않았다.

### npm audit 상태

Command:

```bash
npm audit
```

Result: exit 1. sandbox에서 npm registry DNS 해석이 `ENOTFOUND`로 차단됐고, dependency metadata를 외부 registry로 전송하는 승인 요청도 거부되어 현재 online advisory 결과는 확인하지 못했다. 제품 또는 검증 test 실패가 아닌 실행 환경 제한으로 분류한다.

Command:

```bash
npm audit --offline
```

Result: exit 0, local cache 기준 `found 0 vulnerabilities`. offline cache 결과는 현재 registry advisory 확인을 대체하지 않는다. Task 4의 lockfile 갱신 명령은 npm의 `1 high severity vulnerability` 경고를 보고했으므로, fresh online audit으로 해소가 확인될 때까지 기존 high 취약점을 미확인 잔여 리스크로 유지한다.

## 잔여 이슈

- npm `0.1.1`~`0.1.4` tarball은 수정할 수 없는 불변 공개 artifact이므로 Lucide 코드가 남아 있다.
- 고지와 제거가 반영된 patch publish, tag, GitHub Release는 별도 승인 후 진행한다.
- 이번 작업에서는 publish, tag, GitHub Release, unpublish, history rewrite 또는 remote push를 수행하지 않았다.
- 현재 online npm audit은 실행 환경 제한으로 완료하지 못했다. Task 4에서 보고된 high 1건의 현재 상태는 trusted network 환경의 fresh audit 전까지 미확인이다.
- CRUD·컬럼 이동·대용량 가상 스크롤·테마 GIF 4종 추가는 별도 문서 개선 범위다.
