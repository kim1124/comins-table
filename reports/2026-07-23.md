# 2026-07-23 Multi-column Sort

## 작업 일시

- 2026-07-23 10:24–10:49 KST
- 적용 계약: `COMINS_CONTRACT` v1.2

## 요약

- 기존 단일 정렬 API를 유지하면서 우선순위 기반 `CominsSortModel`을 추가했다.
- `multiSort`를 명시한 경우 `Shift`+click 또는 `Shift`+`Enter`/`Space`로 조건을 추가·변경·제거하고, 일반 조작은 기존 단일 정렬을 유지한다.
- Flat Table과 Tree Grid sibling 정렬, 2Depth child Header, custom comparator, stable tie, Ref 복원, callback 관찰, 컬럼 제거 정규화를 같은 모델에 연결했다.
- Header에 방향 아이콘과 1-based 우선순위 badge를 표시하고, 첫 조건만 `aria-sort`를 사용하며 후속 조건은 숨김 상태 설명으로 보완했다.
- README, 영어·한국어 사용자 문서, API/Ref 예제, Header Playground, 옵션 가이드, changelog에 기능을 명시했다.
- 공개 npm `latest=0.1.3`을 확인하고 release candidate version을 `0.1.4`로 정렬했다.
- 원격 push, publish, tag, GitHub Release는 아직 수행하지 않았다.

## 주요 변경 파일

- 라이브러리: `src/core.ts`, `src/index.tsx`, `styles.css`
- Playground: `example/src/features/HeaderFeature.tsx`, `example/src/features/BodyFeature.tsx`, `example/src/features/featureRegistry.tsx`
- 공개 가이드: `README.md`, `docs/user/03-core-state.md`, `docs/user/06-header.md`, `docs/user/12-playground.md`, `docs/user/17-tree-grid.md` 및 대응 한국어 문서
- API 예제: `example/src/docs/codeSamples.ts`, `example/src/docs/dataTableOptionGuide.ts`
- 테스트: core, public API, Header interaction/renderer, Tree Grid, user docs, Header E2E, virtualization performance specs
- 설계·계획: `docs/superpowers/specs/2026-07-23-multi-column-sort-design.md`, `docs/superpowers/plans/2026-07-23-multi-column-sort.md`

## RED / 수정 근거

- 최초 focused Vitest는 `setCominsSortModel`, Ref 모델 API, 모델 callback이 없어 5건 실패하고 66건 통과했다.
- 컬럼 제거 회귀 테스트는 내부 모델은 정규화됐지만 `onChangeSort`가 호출되지 않아 1건 실패했다. Column 정의 동기화 경로에서 정규화된 단일 projection과 전체 모델을 각각 한 번 통지하도록 수정했다.
- 최초 `npm run verify`는 README 목차 고정 및 기존 Ref 문구 계약 2건에서 실패했다. 새 기능을 기존 `Controlled Model`의 하위 섹션으로 배치하고 단일 정렬 문구를 보존해 문서 계약을 수정했다.
- 최초 10만 Row focused performance 테스트는 Body viewport 내부에서 Header를 찾는 잘못된 locator로 timeout이 발생했다. 상위 `.comins-table` 범위로 locator를 교정했다.

## 검증

- focused Vitest 6 files: 104/104 통과
- README 및 user docs focused Vitest: 25/25 통과
- Header basic/layout Playwright: 25/25 통과
- Header quality Playwright: 7/7 통과
- 100000 Row 3조건 정렬 focused Playwright: 1/1 통과, virtual DOM Row 수 제한 유지
- `npm run verify`: hygiene, 보안 9/9, TypeScript, 전체 Vitest 151/151, production build 통과
- `npm run test:e2e -- --workers=1`: 전체 non-performance Playwright 88/88 통과
- `npm run test:perf -- --workers=1`: 전체 performance Playwright 24/24 통과
- `git diff --check`: 통과

## 실행 환경 분류 및 정리

- focused performance와 전체 performance의 최초 샌드박스 실행은 `127.0.0.1:4002`의 `listen EPERM`으로 테스트 시작 전에 종료됐다. 동일 명령을 승인된 bind 가능 환경에서 재실행해 각각 1/1, 24/24 통과했다.
- 전체 E2E/performance가 생성한 ignored PNG 2개, memory audit JSON 5개, `test-results/.last-run.json`만 생성 시각과 정확한 경로를 확인한 뒤 제거했다.

## 잔여 리스크 및 제외

- 서버 정렬 모델, always-accumulate click, Ctrl/Cmd trigger, 최대 조건 수 prop, initial controlled `sortModel` prop은 이번 범위에 포함하지 않았다.
- Firefox와 Safari의 다중 정렬 UI 직접 검증은 수행하지 않았으며, 현재 검증은 프로젝트 Playwright Chromium 기준이다.
- 원격 CI와 소비자 package artifact 검증은 push 또는 release 범위가 승인될 때 별도로 수행해야 한다.

## 0.1.4 Release Candidate

### 작업 시간

- 2026-07-23 11:02 KST

### 변경

- 공개 npm versions와 dist-tag를 조회해 `0.1.3`이 최신이자 `latest`임을 확인했다.
- `package.json`, `package-lock.json`, `CHANGELOG.md`를 `0.1.4` release candidate로 정렬했다.

### 배포 경계

- `SECURITY.md`에 따라 `.github/workflows/publish.yml`의 OIDC trusted publishing과 `npm stage publish`만 사용한다.
- PR 및 원격 CI 통과, 보호된 `main` 병합, post-merge CI 통과 후 exact version `0.1.4`로 workflow를 시작한다.
- GitHub CLI 인증은 확인 시점에 만료 상태였으며, 원격 쓰기 전에 인증 복구가 필요하다.

### 로컬 release candidate 검증

- `npm run verify` 단계: hygiene, 보안 9/9, TypeScript, Vitest 151/151, production build 통과
- `npm run test:e2e -- --workers=1`: 전체 non-performance Playwright 88/88 통과
- `npm run test:perf -- --workers=1`: 전체 performance Playwright 24/24 통과
- `npm run test:consumer`: 격리 소비자 설치와 root/core/clipboard/selection/styles export smoke 통과
- 격리 npm cache를 사용한 `npm run verify:package-artifact`: `comins-table-0.1.4.tgz` 27개 파일 allow-list 통과
- 로컬 exact artifact SHA-256: `1aa73fb14dfc651599602b8ebf1189c96e92146f79b6c27eb52a3dc2fedf204b`
- `origin/main`과 작업 시작 HEAD가 `5a358584a2bda39f5bd2ee6d72c1e922b4fee707`로 일치함을 fetch 후 확인

### 실행 환경 분류 및 정리

- `verify:full`의 baseline은 통과했지만 최초 E2E server bind가 샌드박스 `listen EPERM`으로 중단됐다. 동일 E2E를 bind 가능 환경에서 1 worker로 재실행해 88/88 통과했다.
- 최초 consumer smoke는 샌드박스 registry 단계에서 출력 없이 지연되어 중단했고, 네트워크 접근 환경에서 재실행해 통과했다.
- 최초 package artifact gate는 기본 npm cache 로그 디렉터리 쓰기 권한으로 실패했다. 격리 `/tmp` cache에서 pack metadata와 exact gate를 재실행해 통과했다.
- E2E/performance가 생성한 PNG 2개, memory audit JSON 5개, Playwright 상태 파일과 로컬 `0.1.4` archive는 정확한 경로를 확인한 뒤 제거했다.
