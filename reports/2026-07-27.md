# 2026-07-27

## 10:32 KST - Lean Comins guidance adoption

### Summary

- Adopted the Governance-managed conditional release-reporting and lean-guidance rules.
- Removed the project-local Semble override so the reviewed global semantic-search server is available without duplicating its launch configuration.
- Kept `gpt-5.6-sol` with `xhigh` reasoning and Plan effort unchanged.
- Preserved all Data Table module guidance and product behavior.

### Changed Files

- `AGENTS.md`
- `reports/2026-07-27.md`

### Validation

- `node --test test/sensitive-data-gates.node.mjs`: 7/7 passed.
- `codex --strict-config --version`: passed on CLI `0.145.0`.
- Managed guidance and configuration match the Governance source.
- `git diff --check`: passed.

### Residual Risks

- A new Codex session is required to observe the global Semble activation.
- Product, build, browser, remote, and release gates were not run because no product behavior changed.

## 11:43 KST - Table code health and consumer Playground validation

### Summary

- Strengthened consumer-facing Playground coverage with controlled Infinite Scroll, Selection & Clipboard, and live Flat Table Ref examples.
- Added browser acceptance for pagination row boundaries, Loading Empty-to-Ready recovery, route lifecycle, mobile overflow, and the new examples.
- Removed the zero-reference `CoreFeature.tsx`; retained `AdvancedFeature.tsx` because documentation tests still consume it.
- Fixed active pointer gesture cleanup for cell range selection, row drag, and header resize. `pointermove`, `pointerup`, `pointercancel`, and `blur` listeners are now removed on completion, cancellation, replacement, and unmount.
- Prevented post-drag compatibility events from overwriting the selected range after pointer release.
- Fixed Infinite Scroll refresh so the application-owned request offset and virtual viewport both restart from the beginning.
- Preserved the public API, package version, dependency graph, package exports, and unsupported-feature boundary.

### Changed Files

- Consumer examples and routing: `example/src/features/InfiniteScrollFeature.tsx`, `example/src/features/SelectionClipboardFeature.tsx`, `example/src/features/RefApiFeature.tsx`, `example/src/features/featureRegistry.tsx`, and related docs route/sample files.
- Public guidance: `README.md`, `docs/user/*`, and matching `docs/ko/*` pages for selection, clipboard, row, header, Playground, and Infinite Scroll behavior.
- Library health: `src/index.tsx`; removed `example/src/features/CoreFeature.tsx`.
- Browser and unit acceptance: focused pagination/loading tests, new Infinite Scroll/Selection/Ref specs, lifecycle and memory audit coverage, visual typography coverage, `test/table-interaction.test.tsx`, and `test/user-docs.test.ts`.
- Approved implementation plan: `docs/superpowers/plans/2026-07-27-code-health-playground-e2e-validation.md`.

### Validation

- `npm run test:e2e -- --list`: 94 non-performance tests in 27 files. The approved estimate was 93; the additional case is the dedicated Selection/Ref visual and root-overflow acceptance test.
- `npm run test:perf -- --list`: 24 performance tests in 7 files.
- `npm run verify`: passed repository hygiene, 9/9 security tests, TypeScript, 16 Vitest files with 155/155 tests, and the production build.
- `CI=1 npm run test:e2e -- --workers=1`: 94/94 passed.
- `CI=1 npm run test:perf -- --workers=1`: 24/24 passed.
- `npm run test:perf -- test/playwright/specs/memory-leak-full-audit.spec.ts --workers=1`: 5/5 passed with the existing 10% threshold unchanged.
- The five memory scenarios returned to 2 documents, 339 JS event listeners, and 952 DOM nodes; post-GC heap was lower than each warmed Basic baseline.
- `npm run test:e2e -- test/playwright/specs/visual-typography.spec.ts --workers=1`: 2/2 passed. Six generated desktop/mobile images were inspected for typography, clipping, control readability, and root horizontal overflow.
- `npm audit --omit=dev --audit-level=high`: found 0 vulnerabilities.
- `git diff --check`: passed.

### Failure Classification

- Product behavior: the first complete E2E run exposed retained virtual scroll position after Infinite Scroll refresh. The controlled example now keys the table and request effect by refresh version; the focused test passed three consecutive runs before the final 94-test gate passed.
- Product behavior during implementation: pointer and compatibility mouse events could race after a cell range drag. Button-state guards and active pointer ownership removed the overwrite; the focused drag test passed five consecutive runs and the related 13-test interaction group passed.
- Test contract: no thresholds or assertions were weakened. The mobile evidence capture height changed from 844px to 1200px while retaining a 390px mobile width so the complete card is visible inside the scroll container.
- Execution environment: sandbox-local Playwright startup could fail with `listen EPERM`, and sandboxed npm audit DNS could fail with `ENOTFOUND`. The same commands passed when rerun with the required local-server or registry permission.

### Memory and Performance Judgment

- No memory leak was detected in the Chromium/CDP scenarios for 100,000-row virtual scroll, component columns, context menus, header/row/cell/size interactions, or the expanded feature lifecycle.
- This memory judgment is limited to the exercised lifecycle and CDP counters; it does not prove absence of every possible leak.
- Non-leak performance coverage passed all 24 cases, including 100,000-row virtualization, physical scrollbar recovery, component virtual-list bounds, sticky/synchronized virtual headers, and 10,000-node Tree Grid virtualization.

### Residual Risks

- Playwright configuration currently validates Chromium only; Firefox, WebKit, Safari, and physical mobile devices were not executed.
- Mobile evidence uses a 390px width and a taller 1200px capture viewport for complete-card review; toolbar and table overflow remain internal scroll surfaces.
- Generated visual and memory artifacts remain in the ignored `reports/artifacts/` tree and are not release artifacts.
- No push, publish, tag, GitHub Release, external write, dependency change, or package version change was performed.
