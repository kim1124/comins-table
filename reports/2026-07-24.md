# 2026-07-24

## 17:22 KST - 0.1.4 post-publication closure

### Summary

- Completed every `comins-table@0.1.4` post-publication closure check under the common Comins policy; merging this evidence to `main` makes the closed record durable.
- Confirmed the protected source merge, pull-request and post-merge verification, trusted staging workflow, public registry metadata, exact public tarball, and isolated React 18 consumer behavior.
- Fixed the future publish path so the consumer smoke receives the same exact artifact that is scanned, uploaded, and staged.
- Fast-forwarded local `main` to the release source merge on `origin/main`.

### Source And Workflow Evidence

- PR #18 head `9228e28` merged to `main` as `d50feff` on 2026-07-23 14:04:53 KST.
- PR Verify run `29980996522` completed successfully.
- Post-merge Verify run `29981190339` completed successfully.
- Trusted Stage npm package run `29981355469` completed successfully from merge `d50feff`.

### Public Registry And Consumer Evidence

- npm published `0.1.4` at 2026-07-24 15:59:51 KST and resolves `latest=0.1.4`.
- Public tarball inspection returned 27 files with shasum `f309a737904eb8beab5143805195fa03a4479470`.
- The downloaded tarball integrity matched the registry `dist.integrity`.
- Registry signature metadata and SLSA provenance v1 attestation are present.
- `npm run test:consumer -- <public-0.1.4-tarball>` installed the public tarball with React 18 and passed the root, core, clipboard, selection, and styles export smoke.
- Local `main` and `origin/main` both resolved to release merge `d50feff` when closure was recorded.

### Exact Artifact Hardening

- Root cause: the Data Table consumer script always created a separate local tarball, and the publish workflow ran it before creating the scanned release artifact.
- `scripts/consumer-smoke.mjs` now accepts an optional exact tarball path.
- `.github/workflows/publish.yml` now passes the scanned pack output to consumer smoke before artifact upload and staging.
- `test/sensitive-data-gates.node.mjs` now requires the exact-artifact workflow order and single consumer invocation.
- The focused security/workflow contract test failed before the change and passed 7/7 after it.

### Validation And Failure Classification

- `publish.yml` parsed successfully with the repository-compatible Ruby YAML parser.
- The public exact-tarball consumer smoke and the existing no-argument local-pack consumer smoke both passed.
- The first `npm run verify` passed hygiene, security 9/9, and TypeScript before Vitest discovered the ignored active `.worktrees/issue-forms-pilot` tree. Its duplicated Vitest files passed, but 29 nested Playwright specs were invalidly collected as Vitest suites.
- Root cause: `vite.config.ts` excluded the root `test/playwright/**` tree but not the repository-local `.worktrees/**` boundary that Git already ignores.
- Added `.worktrees/**` to Vitest exclusion and pinned the boundary in the Node contract test.
- Final `npm run verify` passed hygiene, security 9/9, TypeScript, Vitest 151/151, and production build.

### Residual Risks

- No release verification blocker remains. The release remains published with a pending repository closure record until this evidence is merged to `main`.
- The remaining release source and policy-adoption branches are not deleted; branch and worktree cleanup requires separate maintainer approval.
- Unrelated Summary/Tree Playground and Issue Forms worktrees remain active and were not modified.
- The common policy adoption and exact-artifact hardening are local changes pending their own reviewed pull request.
