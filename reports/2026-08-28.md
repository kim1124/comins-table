# 2026-08-28 0.1.7 Post-Publication Closure

- 작업 일시: 2026-08-28 10:51 KST
- 작업 브랜치: `codex-0-1-7-release-closure`
- 대상 버전: `comins-table@0.1.7`
- 배포 상태: 공개 완료, 본 종료 증거의 보호된 `main` 병합과 default branch reconciliation 대기

## 요약

- 보호된 source merge, 병합 후 CI, trusted staging, maintainer 2FA 승인, 공개 registry와 exact public artifact를 독립적으로 확인했다.
- npm은 2026-08-28 10:38:47 KST에 `comins-table@0.1.7`을 공개했고 `latest=0.1.7`로 해석한다.
- 공개 artifact의 staged digest, registry integrity, registry signature, SLSA provenance와 React 18 consumer 동작이 모두 일치했다.
- 이 종료 증거가 보호된 `main`에 병합되고 로컬 default branch가 원격과 일치하면 `0.1.7` release state는 Closed다.

## Source 및 Workflow 근거

- release PR #51의 head `15b94d51be7703f2f4838eb4ce2a9bac367f745d`는 2026-08-27 19:08:04 KST에 merge commit `39831464a10a9933bb464592e1021a17667aff4f`로 `main`에 병합됐다.
- 같은 merge SHA의 post-merge Verify run `33061700348`과 CodeQL run `33061700472`은 모두 성공했다.
- trusted Stage npm package run `33062191635`은 같은 merge SHA에서 `verify-and-pack`과 `stage` job을 모두 통과했다.
- stage job은 GitHub `npm` environment 승인 뒤 current npm maintainer gate를 값 비노출 방식으로 통과하고, OIDC trusted publisher로 exact artifact에 `npm stage publish`를 수행했다.
- maintainer는 stage ID `6d209424-77d8-427b-beed-c53465be400a`를 npm 2FA로 별도 승인했다.

## Maintainer 및 Publisher Identity 근거

- exact `0.1.7` maintainer는 정확히 1개이고 현재 package maintainer와 동일하며 승인된 공개 handle과 일치한다. 실제 identity 값과 address는 출력하거나 저장하지 않았다.
- exact publisher는 GitHub Actions OIDC service identity이며, provenance의 repository, workflow, ref, merge SHA와 workflow invocation이 trusted publisher 설정과 일치한다.
- npm maintainer와 GitHub Actions publisher는 서로 다른 역할이므로 provider metadata 값이 서로 다른 것은 trusted publishing의 정상 구조다.
- 최초 임시 검사는 두 역할의 email 값이 같아야 한다고 잘못 가정했고, 인증되지 않은 로컬 npm profile 조회도 함께 요구하여 fail-closed로 종료됐다. 역할별 provider metadata와 provenance를 분리한 최종 값 비노출 검사는 통과했으며 release 설정 불일치는 확인되지 않았다.

## 공개 Registry 및 Artifact 근거

- 공개 registry는 exact version `0.1.7`, dist-tag `latest=0.1.7`, 40개 파일과 unpacked size 837,966 bytes를 반환했다.
- 공개 tarball shasum은 `8b4510107faa4f2758e3e11337b3d69fa8cb64ec`이며 stage workflow가 출력한 exact artifact shasum과 일치했다.
- 공개 tarball SHA-512는 `57b054544de3e97b8edbefded9286bcfe556cee547f0c28a848360a2b936f5e2d3d5b061443e829d41098ba20c3e920444872cb5ad685228515afb2edc16cd69`이며 pre-release exact artifact와 일치했다.
- 공개 tarball을 registry에서 새로 다운로드해 40개 파일, SHA-1과 SHA-512 byte digest를 재검증했다.
- 격리 설치의 `npm audit signatures`는 7개 package registry signature와 `comins-table@0.1.7` attestation 1개를 검증했으며 invalid 또는 missing 항목은 없었다.
- SLSA provenance는 repository `kim1124/comins-table`, `.github/workflows/publish.yml`, `refs/heads/main`, merge SHA `39831464a10a9933bb464592e1021a17667aff4f`, run `33062191635`, exact artifact SHA-512를 가리켰다.
- `npm run test:consumer -- <public-0.1.7-tarball>`은 격리 React 18 consumer에서 root, core, clipboard, selection, styles와 Radix peer resolve smoke를 통과했다.

## Security 및 Repository 상태

- GitHub Dependabot, Code Scanning, Secret Scanning open alert는 각각 0건이다.
- release source merge 시점의 `verify:full`은 security 21개, license 27개, Vitest 333개, TypeScript, production build, Chromium E2E 120개를 통과했다.
- `main`, `origin/main`, release workflow head는 종료 증거 작성 시작 시점에 모두 `39831464a10a9933bb464592e1021a17667aff4f`로 일치했다.
- Git tag와 GitHub Release는 기존 release 관례에 따라 생성하지 않았다.

## 검증 및 실패 분류

- `npm view` exact-version 검사: `0.1.7`, `latest=0.1.7`, integrity, signature와 SLSA provenance metadata 확인.
- 공개 exact tarball SHA-1/SHA-512 및 40개 파일: stage artifact와 일치.
- `npm run verify:npm-maintainer`: 값 비노출 current maintainer gate 통과.
- `npm run test:consumer -- <public-0.1.7-tarball>`: 격리 consumer smoke 통과.
- `npm audit signatures`: registry signature 7개와 attestation 1개 검증, 취약점 0건.
- closure report 반영 후 `npm run check:hygiene`: 통과.
- closure report 반영 후 `npm run test:security`: 21/21 통과.
- 최초 provenance fail-closed parser는 workflow path 정규식의 shell escaping 오류로 실행 전에 exit 1이었다. 이스케이프에 의존하지 않도록 비교식을 수정한 재실행에서 digest, repository, workflow, ref, merge SHA와 run ID 전체 비교가 통과했다. 제품, artifact 또는 provenance 실패가 아니다.
- 최초 identity 비교의 fail-closed 결과는 maintainer와 OIDC publisher가 동일 email이어야 한다는 잘못된 검사 계약과 인증되지 않은 npm profile 조회가 원인이었다. public exact/current maintainer와 OIDC publisher 역할을 분리한 값 비노출 재검증은 통과했다.

## 잔여 리스크

- 브라우저 제품 검증은 release source의 Chromium E2E 기준이며 Firefox, WebKit, 실제 Safari와 물리 모바일 기기는 이번 closure에서 추가 실행하지 않았다.
- Git tag와 GitHub Release는 생성하지 않았다.
- `/private/tmp/comins-v17-data-table-privacy-20260824`의 `codex/privacy-remediation-closure` worktree는 별도 사용자 작업으로 보존했고 읽기 전용 상태 확인 외에는 변경하지 않았다.
- `codex-0-1-7-release-closure` 브랜치와 기존 privacy worktree/branch 삭제는 별도 maintainer 승인 범위다.
- 이 보고서의 보호된 PR, 병합 후 CI와 local/remote default branch reconciliation이 완료되기 전까지 release state는 Published다.

# 2026-08-28 0.1.8 Column Pinning 및 Cross-Table Drag 로컬 구현

- 작업 일시: 2026-08-28 16:12 KST
- 작업 브랜치: `codex-0-1-8-transfer-pinning`
- 기준 커밋: `2439e10c4fe9d55a0018fa69c5adaeea6697b902`
- 대상 버전: `comins-table@0.1.8`
- 상태: 로컬 구현 및 검증 완료, commit 및 원격 작업 미수행

## 요약

- Column과 Header Group의 configured left/right pin, position lock, sticky offset, responsive demotion/restoration 및 layout persistence를 구현했다.
- Header, Body, Skeleton과 Summary에 같은 effective pin zone을 적용하고 Summary `colSpan`을 zone 경계에서 fragment로 분리했다. full-width Group Row와 Row Detail은 single non-sticky Cell을 유지한다.
- application-created Coordinator를 공유하는 flat/grouped Table 사이에서 Row를 이동하고, Group과 모든 member Row를 하나의 bundle로 이동하는 controlled Cross-Table Drag를 구현했다.
- 중복 Row/Group ID는 기본 reject이며 target `resolveConflict`가 명시한 overwrite만 허용한다. target `canTransfer`는 conflict resolution 전에 이동을 거부한다.
- Group Row 이동은 source Group을 제거하고, 마지막 member Row만 이동하면 source의 빈 Group을 유지한다.
- target Table만 움직이는 vertical edge auto-scroll, 최신 target 재해석, cancel/blur/Escape/unmount cleanup과 destination focus 복구를 구현했다.
- Playground, README, CHANGELOG, 한·영 user docs, option guide, code sample과 package/lockfile 0.1.8을 동기화했다.

## 주요 변경 파일

- Library: `src/column-pinning.ts`, `src/drag-autoscroll.ts`, `src/table-transfer.ts`, `src/core.ts`, `src/index.tsx`, `styles.css`
- Playground: `example/src/features/ColumnPinningFeature.tsx`, `example/src/features/CrossTableDragFeature.tsx`, registry/types/routes/code samples/option guide/style
- Documentation: `docs/user/22-column-pinning.md`, `docs/ko/22-column-pinning.md`, `docs/user/23-cross-table-drag.md`, `docs/ko/23-cross-table-drag.md`, `README.md`, `CHANGELOG.md`
- Tests: Column Pinning, transfer/autoscroll pure tests, core/component/type fixtures, docs/localization tests, Column Pinning 및 Cross-Table Playwright specs
- Planning/version: `docs/plans/archive/2026-08-28-v0.1.8-column-pinning-cross-table-drag.md`, `package.json`, `package-lock.json`

## 검증

- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 361/361, production build 통과
- `npx vite build --config vite.example.config.ts`: Playground build 통과, 기존 500 kB chunk-size warning만 발생
- `npm run test:e2e -- --workers=1`: Chromium non-performance 125/125 통과
- `npm run test:perf -- --workers=1 test/playwright/specs/virtualization.spec.ts`: focused virtualization 9/9 통과
- `npm run test:perf -- --workers=1`: 전체 performance 및 memory 28/28 통과
- `npm run verify:package-artifact`: 격리 npm cache에서 47-file `comins-table-0.1.8.tgz` 통과
- `npm run test:consumer -- comins-table-0.1.8.tgz`: 동일 exact artifact의 React 18 consumer smoke 통과
- `git diff --check`: 통과
- 검증용 `comins-table-0.1.8.tgz`와 변경 전 HEAD A/B 임시 디렉터리는 검증 후 정확한 대상만 제거했다.

## 실패 분류 및 수정 근거

- Cross-Table 이동 후 target data는 3개였지만 `pagination` 미지정 Table이 초기 2개 Row의 내부 pageSize를 보존하여 세 번째 Row를 숨겼다. 무페이지네이션 controlled data 증가 시 전체 Row 수를 다시 반영하도록 수정하고 component regression을 추가했다.
- 기존 local Row Drag의 pointerup이 렌더된 placeholder 위에 놓이면 신규 cross-target fallback이 직전 local target을 invalid로 덮어썼다. 같은 Table 내부 placeholder에서는 직전 유효 target을 보존하도록 수정했다.
- target edge auto-scroll의 첫 animation frame이 delta 0을 이동 없음으로 처리해 loop를 즉시 종료했다. 첫 frame은 timestamp 기준만 설정하고 다음 frame을 예약하도록 수정했으며 실제 long target Table E2E로 확인했다.
- 최초 전체 unit 실패 6건은 신규 Playground Feature ID/번역/README heading 기준 목록 누락이었다. 신규 공개 항목을 canonical order와 localization contract에 등록해 해결했다.
- 최초 package artifact 검사는 sandbox 기본 npm cache/log 경로 제한으로 fail-closed 됐다. `/tmp` 격리 cache에서 동일 검사와 exact consumer smoke가 통과하여 실행 환경 실패로 분류했다.
- physical-scrollbar memory test는 한 실행에서 변경본 1,223→3,050 nodes, 변경 전 HEAD 1,213→3,008 nodes로 동일 실패했다. 양쪽 모두 `documents=2`, live element와 heap/event listener는 기준 범위였으므로 신규 기능 누수가 아닌 현 브라우저의 retained document counter 변동으로 분류하고 threshold를 유지했다. 최종 전체 performance 실행은 baseline 3,047, 복귀 3,050으로 28/28 통과했다.
- 초기 auto-scroll E2E 좌표는 target bottom이 browser viewport 밖에 있어 hit target이 없었다. 실제 geometry를 측정하고 target edge가 화면에 보이는 viewport 조건을 명시해 assertion을 유지했다.

## 잔여 리스크

- 브라우저 제품 검증은 Chromium 기준이다. Firefox, WebKit과 실제 Safari는 이번 범위에서 실행하지 않았다.
- Tree Grid Column Pinning/Drag, flat↔grouped 변환, batch/copy Drag, filtered/infinite/lazy Cross-Table Transfer와 asynchronous conflict dialog는 지원하지 않는다.
- application은 Coordinator `onTransfer`에서 source와 target controlled model을 함께 반영해야 한다. 한쪽만 반영한 부분 상태를 library가 자동 복구하지 않는다.
- CSS sticky의 stacking/background는 Chromium에서 검증했으며 다른 브라우저 지원을 주장하지 않는다.
- 새 runtime dependency는 추가하지 않았다.
- commit, remote push, PR, merge, tag, GitHub Release와 npm publish는 수행하지 않았다.

# 2026-08-28 Column Pinning Playground 및 pinned resize 보정

- 작업 일시: 2026-08-28 18:14 KST
- 작업 브랜치: `codex-0-1-8-transfer-pinning`
- 기준 커밋: `2ed8d22`
- 상태: 로컬 수정 및 핵심 검증 완료, commit 및 원격 작업 미수행

## 요약

- Column Pinning Playground의 지정 폭 합계가 실제 Table container보다 작아 수평 overflow가 없던 예제를 12개 fixed-width Column, 총 약 1,880px로 확장했다.
- Effective pinned Column 또는 Header Group의 직접 resize가 자기 자신을 responsive demotion시키지 않도록, 다른 effective pinned block과 center 48px를 제외한 가용 폭을 gesture 상한으로 적용했다.
- 독립적인 browser/parent container 축소 시 기존 wider-side inner-block demotion 정책과 configured layout intent는 유지했다.
- Row/Group Drag 구현은 변경하지 않았고 focused Cross-Table Drag E2E로 회귀를 확인했다.
- README, CHANGELOG, 한·영 Column Pinning guide와 Playground 설명을 동기화했다.

## 주요 변경 파일

- Library: `src/column-pinning.ts`, `src/index.tsx`
- Playground: `example/src/features/ColumnPinningFeature.tsx`
- Documentation: `README.md`, `CHANGELOG.md`, `docs/user/22-column-pinning.md`, `docs/ko/22-column-pinning.md`, 기존 Column Pinning design 및 0.1.8 implementation plan
- Tests: `test/column-pinning.test.ts`, `test/playwright/specs/column-pinning.spec.ts`

## 검증

- Failing-first Column Pinning E2E에서 수정 전 `clientWidth=932`, `scrollWidth=932` 및 pinned Name resize 후 `data-comins-pinned` 제거를 재현했다.
- Column Pinning unit: 6/6 통과
- Column Pinning focused Chromium E2E: horizontal overflow, 실제 center Cell 이동, left/right sticky 좌표, pinned Column/Header Group resize 상한과 responsive demotion 3/3 통과
- 한·영 Playground/localization/user docs focused Vitest: 60/60 통과
- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 363/363, library production build 통과
- `npx vite build --config vite.example.config.ts`: Playground production build 통과, 기존 500kB chunk-size warning만 발생
- Cross-Table Row/Group Drag focused Chromium E2E: 4/4 통과
- `npm run test:e2e -- --workers=1`: Chromium non-performance 127/127 통과
- `npm run test:perf -- --workers=1 test/playwright/specs/virtual-sticky-header.spec.ts`: horizontal overflow, resize와 scroll sync 포함 4/4 통과
- `npm run test:perf -- --workers=1`: 27/28 통과. physical-scrollbar retained node counter 1건은 아래와 같이 분류했다.
- `git diff --check`: 통과

## 실패 분류 및 수정 근거

- 최초 focused/full performance 명령의 `listen EPERM 127.0.0.1:4002`는 제한된 sandbox의 localhost bind 실패였다. 동일 명령을 허용된 localhost 환경에서 재실행했다.
- 전체 performance와 독립 physical-scrollbar 재실행 모두 `basicBaseline nodes=1,223`, `afterBasic nodes=3,050`, `documents=2`로 동일 실패했다. 이 값은 변경 전 기록의 실패값과 정확히 같고 live element, heap과 event listener는 허용 범위였으며, Column Pinning 경로의 focused performance 4/4는 통과했다. 따라서 현 Chromium의 retained document counter 변동으로 분류하고 threshold/assertion은 변경하지 않았다.

## 잔여 리스크

- 전체 performance gate는 환경성 physical-scrollbar counter 1건 때문에 최종 green이 아니다. 해당 실패를 제품 통과로 과장하지 않는다.
- 실제 browser 검증은 Chromium 기준이다. Firefox, WebKit과 실제 Safari는 이번 변경에서 실행하지 않았다.
- 신규 runtime dependency, public API, commit, remote push, PR, merge, tag, GitHub Release와 npm publish는 추가하지 않았다.

# 2026-08-28 Cross-Table Duplicate ID Tooltip Feedback

- 작업 일시: 2026-08-28 17:53 KST
- 작업 브랜치: `codex-0-1-8-transfer-pinning`
- 기준 커밋: `2ed8d22`
- 상태: 로컬 구현 및 검증 완료, 신규 commit 및 원격 작업 미수행

## 요약

- Duplicate Row/Group ID 정책이 reject로 확정된 pointer Drop에 compact한 Pointer Tooltip과 target Table outline을 표시한다.
- Tooltip은 reusable internal `CominsTooltipSurface`와 `CominsPointerTooltip`로 분리했으며 pointer 좌표 12px offset, viewport collision 보정, `pointer-events: none`, polite live status와 자동 해제를 적용했다.
- `tableTransfer.rejectionFeedback`으로 target-owned Tooltip body renderer와 500~10000ms duration을 지정하거나 기본 feedback을 끌 수 있다.
- `Coordinator.onTransferRejected`는 application Toast 또는 기록 처리를 위해 duplicate conflict, transfer kind, reason과 source/target Table ID를 전달한다.
- Playground에는 승인된 deep burgundy 디자인과 한·영 message renderer를 적용했고 README, CHANGELOG, 한·영 user docs와 Option Guide를 동기화했다.
- 새 runtime dependency 또는 Radix export를 추가하지 않았다. Warning mark는 module-owned CSS icon이다.

## 주요 변경 파일

- Library: `src/tooltip.tsx`, `src/table-transfer.ts`, `src/index.tsx`, `styles.css`
- Playground: `example/src/features/CrossTableDragFeature.tsx`, Option Guide와 Feature registry
- Documentation: `README.md`, `CHANGELOG.md`, `docs/user/23-cross-table-drag.md`, `docs/ko/23-cross-table-drag.md`
- Tests: transfer/component/type/docs unit contract와 `test/playwright/specs/cross-table-drag.spec.ts`

## 검증

- 관련 Vitest: transfer, interaction, icon, docs 177/177 통과
- Localization focused Vitest: 54/54 통과
- Cross-Table Drag focused Chromium E2E: 4/4 통과
- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 362/362, library production build 통과
- `npx vite build --config vite.example.config.ts`: Playground production build 통과, 기존 500kB chunk-size warning만 발생
- `npm run test:e2e -- --workers=1`: Chromium non-performance 125/125 통과
- Browser contract는 Tooltip 문구와 visibility, target root outline, computed `pointer-events: none`, Tooltip 위치 아래의 실제 target Table hit-test와 overwrite 전환을 확인한다.

## 실패 분류 및 수정 근거

- 최초 focused E2E는 target outline 속성을 Body viewport에서 찾은 test locator 오류였다. 실제 속성 owner인 Table root로 assertion을 교정한 동일 spec은 4/4 통과했다.
- 최초 전체 license gate는 신규 Radix warning export가 exact approved source inventory를 변경해 fail-closed됐다. 외부 승인 범위를 넓히지 않고 module-owned CSS icon으로 대체했으며 license 27/27과 current repository gate가 통과했다.
- 다음 전체 unit gate는 동일한 `Duplicate ID` 문구도 localization pair를 요구하는 Playground contract 1건에서 중단됐다. `defineLocalizedText` pair로 등록한 뒤 focused 54/54와 최종 Vitest 362/362가 통과했다.
- 전체 E2E 최초 명령은 수동 시각 점검용 Vite 서버가 4002를 점유해 Playwright webServer 시작 전에 종료됐다. 해당 서버만 종료한 동일 명령은 125/125 통과했다.
- Playwright CLI 수동 visual session은 wrapper가 출력 없이 대기하여 중단했다. 시각 동작 주장은 저장소 표준 Chromium E2E의 실제 DOM/style/hit-test evidence로 한정한다.

## 잔여 리스크

- 실제 browser 검증은 Chromium 기준이다. Firefox, WebKit, 실제 Safari와 물리 mobile은 이번 변경에서 실행하지 않았다.
- `onTransferRejected`와 기본 Tooltip은 현재 duplicate conflict가 reject된 pointer Drop에 한정한다. `canTransfer()` 거부는 계속 application-owned 정책이며 notification을 자동 발생시키지 않는다.
- Tooltip Surface는 재사용 가능한 internal component이지만 general hover/focus Tooltip public API는 이번 범위에 포함하지 않았다.
- commit, remote push, PR, merge, tag, GitHub Release와 npm publish는 수행하지 않았다.

# 2026-08-28 Summary 하단 horizontal scrollbar 이동

- 작업 일시: 2026-08-28 18:44 KST
- 작업 브랜치: `codex-0-1-8-transfer-pinning`
- 기준 커밋: `2ed8d22`
- 상태: 로컬 구현 및 핵심 검증 완료, 신규 commit 및 원격 작업 미수행

## 요약

- Body viewport가 소유하던 native horizontal scrollbar를 전체 Table 최하단의 전용 rail로 이동했다.
- Summary를 설정하면 rail은 Summary 다음에 표시되고, Summary가 없으면 Body 바로 다음에 표시된다.
- native scrollbar chrome이 자동으로 숨겨져도 마지막 visual Row의 하단 경계가 유지되도록 12px rail 내부에 1px 상단 구분선을 추가했다.
- Bottom rail 직접 입력과 Body의 trackpad/Shift-wheel 입력을 Header, Body, Summary와 양방향 `scrollLeft` 동기화한다.
- Body는 vertical scroll과 virtualization을 계속 소유하며 Column Pinning offset, Row/Group Drag와 public API는 변경하지 않았다.
- README, CHANGELOG, 한·영 Summary/Virtualization/Column Pinning guide, Playground 설명과 내부 design/implementation plan을 동기화했다.

## 주요 변경 파일

- Library: `src/index.tsx`, `styles.css`
- Playground: `example/src/features/ColumnPinningFeature.tsx`
- Documentation: `README.md`, `CHANGELOG.md`, `docs/user/11-virtualization.md`, `docs/ko/11-virtualization.md`, `docs/user/18-summary-row.md`, `docs/ko/18-summary-row.md`, `docs/user/22-column-pinning.md`, `docs/ko/22-column-pinning.md`, Column Pinning design 및 0.1.8 implementation plan
- Tests: Column Pinning, Playground layout, virtual sticky Header와 physical scrollbar Playwright specs

## 검증

- Failing-first Column Pinning E2E에서 Summary 다음 `table-horizontal-scrollbar`가 존재하지 않는 상태를 재현했다.
- 격리된 headed Chromium에서 Summary bottom과 rail top이 모두 `718.640625px`, rail `1060×12px`, `scrollWidth=1880px`임을 확인했다.
- 같은 browser에서 Body horizontal wheel 입력 후 Header/Body/Summary/rail `scrollLeft=240px` 동기화를 확인했다.
- 최초 Column Pinning focused Chromium E2E: 3/3 통과
- 하단 경계 회귀 테스트를 추가했으며 수정 전 rail의 `border-top-width=0px` 실패를 재현한 뒤 Column Pinning focused Chromium E2E 4/4 통과
- Playground scrollbar style/border focused Chromium E2E: 통과
- 한·영 Playground/localization/user docs focused Vitest: 54/54 통과
- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 363/363, library production build 통과
- `npx vite build --config vite.example.config.ts`: Playground production build 통과, 기존 500kB chunk-size warning만 발생
- `npm run test:e2e -- --workers=1`: Chromium non-performance 127/127 통과
- Virtual Header/resize/horizontal sync focused performance: 4/4 통과
- 최종 `npm run test:perf -- --workers=1`: 27/28 통과. 기존 physical-scrollbar retained document counter 1건만 아래와 같이 실패했다.
- `git diff --check`: 통과

## 실패 분류 및 수정 근거

- 첫 전체 performance에서 기존 bottom-border 테스트가 Body bottom과 Table root bottom 일치를 요구해 새 12px rail만큼 `13px` 차이로 실패했다. Rail bottom과 root bottom, Body와 rail의 연속성을 검증하도록 새 DOM 계약에 맞췄다.
- 첫 physical drag 실행은 helper가 max scroll의 `98.5%`에서 조기 반환하면서 후속 `99.9%` Row assertion과 충돌해 마지막 index `99,294`에서 멈췄다. 제품 범위나 assertion을 완화하지 않고 helper 목표를 `99.9%`로 강화했으며 이후 `bottomIndex=99,999`를 확인했다.
- 최종 전체 performance의 retained node counter는 이전 실행과 같은 `basicBaseline=1,223`, `afterBasic=3,050`, `documents=2`였다. Live element, heap, event listener와 신규 scroll/resize 성능 항목은 기준 내였으므로 기존 Chromium retained-document 변동으로 분류하고 threshold는 유지했다.
- 제한된 sandbox의 focused performance 실행은 `listen EPERM 127.0.0.1:4002`로 시작하지 못했다. 동일 명령을 localhost 허용 환경에서 재실행해 4/4 통과했다.

## 잔여 리스크

- 전체 performance gate는 기존 환경성 physical-scrollbar counter 1건 때문에 최종 green이 아니다.
- 실제 browser 검증은 Chromium 기준이다. Firefox, WebKit, 실제 Safari와 물리 touch horizontal gesture는 이번 변경에서 실행하지 않았다.
- 수동 검증용 server, named browser session, snapshot과 screenshot artifact는 검증 후 종료·제거했다.
- 신규 runtime dependency, public API, commit, remote push, PR, merge, tag, GitHub Release와 npm publish는 추가하지 않았다.

# 2026-08-28 PR #53 교체 및 E2E 실행 계약 보정

- 작업 일시: 2026-08-28 23:21 KST
- 작업 브랜치: `codex-0-1-8-transfer-pinning-2`
- 기준 커밋: `2439e10`
- 상태: 기존 PR 폐기, PR #54 생성, 수평 viewport 보정 로컬 검증 완료, GitHub CI 재검증 대기

## 요약

- CI 환경 대응용 진단 변경이 누적된 PR #53을 닫고, `origin/main`에서 기능 커밋 2건만 재구성한 신규 브랜치를 생성했다.
- Column Pinning E2E가 Body와 하단 rail의 네이티브 스크롤바 수치가 같다고 가정하지 않도록 보정했다.
- 대신 Header, Body, Summary, 하단 rail의 실제 `scrollLeft` 동기화와 Body 및 하단 rail의 최대 수평 스크롤 도달을 검증한다.
- Body의 실제 `clientWidth`를 Header, Summary와 하단 rail의 공통 horizontal viewport 폭으로 사용하고, 세로 스크롤을 소유하지 않는 rail의 중복 gutter를 제거했다.
- 공유 Playground 상태를 사용하는 Playwright E2E는 병렬 간섭을 피하도록 전역 단일 워커로 고정했다.

## 주요 변경 파일

- Library: `src/index.tsx`, `styles.css`
- Test configuration: `playwright.config.ts`
- Browser contract: `test/playwright/specs/column-pinning.spec.ts`, `test/playwright/specs/playground-layout-polish.spec.ts`
- Public history: `CHANGELOG.md`
- Report: `reports/2026-08-28.md`

## 검증

- Column Pinning focused Chromium E2E: 4/4 통과
- Cross-Table Drag focused Chromium E2E: 4/4 통과
- Playground layout focused Chromium E2E: 17/17 통과
- Public user docs focused Vitest: 29/29 통과
- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 363/363, library production build 통과
- 최종 전체 Chromium non-performance E2E: `128 tests using 1 worker`, 128/128 통과
- Virtual Header/resize/horizontal sync focused performance: 4/4 통과
- 전체 performance: 26/28 통과. physical scrollbar retained-document counter와 wheel 평균 기준 2건은 아래와 같이 실패했다.
- 실패한 physical scrollbar focused 재실행: 0/2 통과
- `git diff --check`: 통과

## 실패 분류 및 수정 근거

- PR #53의 네 차례 Chromium E2E는 모두 2개 워커로 실행됐고, 네 차례 모두 Column Pinning 1건이 재시도까지 실패했다. 이는 병렬 실행 문제가 아니라 네이티브 스크롤바의 Body와 rail 폭 차이를 동일해야 한다고 본 테스트 계약 문제였다.
- PR #53 마지막 실행에서 Cross-Table Row Drag와 Group Drag 2건은 첫 시도 실패 후 재시도에서 통과해 flaky로 기록됐다. 동일 기능의 focused E2E와 전체 E2E는 단일 워커에서 모두 통과했다.
- PR #54 최초 Chromium E2E는 단일 워커에서 127/128 통과했지만, 하단 rail을 끝까지 이동해도 Body가 자체 최대 수평 위치보다 15px 앞에서 멈춰 Column Pinning 1건이 재시도까지 실패했다. 이는 assertion 문제가 아니라 서로 다른 horizontal viewport 폭을 최소 공통 범위로 제한한 제품 동작 문제로 분류했다.
- Header에는 table 경계로 인한 2px 추가 overflow가 있으므로 Header 자체 최대값을 강제하지 않는다. Body와 rail은 각각 자체 끝에 도달하고 네 surface의 실제 `scrollLeft`는 같아야 한다는 동작 계약을 유지한다.
- 기존 Playground layout assertion은 Body와 rail 모두 stable gutter를 가져야 한다고 요구했다. Body는 stable을 유지하고 rail은 auto를 사용하도록 역할별 assertion으로 교정했다.
- 최초 로컬 focused E2E는 제한 환경의 `listen EPERM 127.0.0.1:4002`로 시작되지 않았다. 별도 환경을 추가하지 않고 기존 Playground 서버를 재사용한 동일 명령으로 검증했다.
- 전체 performance의 retained-document counter는 전체와 focused 재실행 모두 nodes 1,223→3,051, documents 1→2로 기존 실패값과 동일했다. threshold는 변경하지 않았다.
- wheel 반응성은 전체 실행 평균 51.04ms, focused 재실행 평균 49.21ms로 48ms 기준을 초과했다. 두 실행 모두 timeout은 0이고 p95는 기준 내였으나 반복 실패했으므로 환경성으로 단정하지 않았다.

## 잔여 리스크

- 실제 browser 검증은 Chromium 기준이며 Firefox, WebKit, 실제 Safari는 실행하지 않았다.
- 전체 performance gate는 26/28로 green이 아니다. retained-document counter 1건은 기존 반복 실패이고, wheel 평균 1건은 이번 세션에서 반복 실패한 미해결 리스크다.
- PR #54의 첫 CI는 실패했으며 수평 viewport 보정 후 GitHub Chromium E2E 재검증이 필요하다.
- 신규 dependency, public API, merge, tag, GitHub Release와 npm publish는 추가하거나 수행하지 않았다.
