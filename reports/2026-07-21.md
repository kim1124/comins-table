# 2026-07-21

## Linux CI History Scanner Heap Safety

### Work Time

- `2026-07-21 00:00-00:24 KST`

### Summary

- Replaced synchronous binary-output capture in the reachable-history scan with incremental `git cat-file --batch` streaming.
- Preserved the complete reachable-history scan and the `128`-object upper bound for each content batch.
- Added regressions for both the bounded batch size and the required streaming process boundary.

### Root Cause Classification

- EXECUTION ENVIRONMENT / IMPLEMENTATION INTERACTION: GitHub Actions run `29752914758` failed twice with exit code `134` and glibc heap corruption while the hygiene scanner processed reachable history.
- INSUFFICIENT FIRST MITIGATION: limiting content requests to `128` objects passed local Linux reproduction but run `29754112830` still aborted with `malloc(): invalid next size (unsorted)` while using synchronous binary-output capture.
- IMPLEMENTED BOUNDARY: reachable object content now flows through an asynchronous child-process stream and is parsed incrementally instead of being returned as a native synchronous output buffer.
- UNRESOLVED UPSTREAM DETAIL: the precise Node.js, glibc, or hosted-runner allocator mechanism is not proven locally; the repository removes the synchronous capture path shared by all three remote failures.

### Changed Files

- `scripts/check-repository-hygiene.mjs`
- `test/repository-hygiene.test.ts`
- `reports/2026-07-21.md`

### Verification

- RED/PASS: the bounded-batch regression failed before the first mitigation and passed after the `128`-object boundary was added.
- RED/PASS: the streaming-boundary regression rejected synchronous `git cat-file --batch` capture before the second mitigation and passed after the streamed implementation.
- PASS: all `33` repository hygiene tests passed.
- PASS: the production scanner completed against both the branch HEAD and GitHub PR `#8` merge SHA, scanning `1,017` reachable history objects.
- PASS: the streamed scanner completed against the PR merge SHA in x86_64 Ubuntu 24.04, glibc 2.39, and Node.js 24.14.0.
- PASS: the fresh full local gate completed with hygiene validation, TypeScript, `14` test files, `138` tests, the production build, and `81` E2E tests.
- PENDING: updated GitHub PR checks after the streamed fix is committed and pushed.

### Residual Risks

1. GitHub PR `#8` remains blocked until the streamed implementation passes both required checks.
2. The underlying hosted-runner allocator defect is outside this repository; the precise upstream mechanism remains unconfirmed even though the repository no longer uses the repeatedly failing synchronous capture path.
3. npm provider-retained identity metadata and historical remote comparison copies remain separate follow-up decisions.
