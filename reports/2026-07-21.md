# 2026-07-21

## Linux CI History Scanner Heap Safety

### Work Time

- `2026-07-21 00:00-00:11 KST`

### Summary

- Fixed deterministic Linux Node.js 24 heap corruption in the repository hygiene history scan.
- Preserved the complete reachable-history scan while limiting each `git cat-file --batch` content request to `128` objects.
- Added a real Git-wrapper regression test that rejects any history content batch above the safe object count.

### Root Cause Classification

- EXECUTION ENVIRONMENT / IMPLEMENTATION INTERACTION: GitHub Actions run `29752914758` failed twice with exit code `134` and glibc heap corruption while the hygiene scanner processed reachable history.
- CONFIRMED TRIGGER: the current repository supplied `1,010` blob and tree objects in one content batch with a `6,681,873` byte response. The same scanner reproduced the abort in a read-only Debian Linux Node.js 24 container.
- CONFIRMED FIX BOUNDARY: an otherwise identical temporary experiment using `128`-object batches completed with exit code `0` and scanned the same `1,010` objects.

### Changed Files

- `scripts/check-repository-hygiene.mjs`
- `test/repository-hygiene.test.ts`
- `reports/2026-07-21.md`

### Verification

- RED: the new bounded-batch regression test failed because the existing scanner sent more than `128` object IDs to one `git cat-file --batch` process.
- PASS: the focused regression test passed after the bounded-batch implementation.
- PASS: all `32` repository hygiene tests passed.
- PASS: the updated production scanner completed in the same Debian Linux Node.js 24 container and scanned `1,010` reachable history objects without allocator errors.
- PASS: the full local non-browser gate passed with hygiene validation, TypeScript, `14` test files, `137` tests, and the production build.
- ENVIRONMENT: the first E2E invocation was blocked by sandboxed `127.0.0.1:4002` binding with `listen EPERM`; product behavior was not reached.
- PASS: the network-enabled E2E rerun completed with `81` tests passed.
- PENDING: updated GitHub PR checks after the fix is pushed.

### Residual Risks

1. GitHub PR `#8` remains blocked until the updated commit passes both required checks.
2. The underlying Node.js or glibc allocator defect is outside this repository; the scanner now avoids its confirmed large-batch trigger.
3. npm provider-retained identity metadata and historical remote comparison copies remain separate follow-up decisions.
