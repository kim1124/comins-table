# 2026-07-20

## Repository Security and Privacy Hardening

### Work Time

- `2026-07-20 17:42-17:52 KST`

### Summary

- Pinned `actions/checkout` and `actions/setup-node` to the immutable commits referenced by their official `v6` tags.
- Added ignore rules for local environment overrides and npm credentials while keeping `.env.example` trackable.
- Replaced the bootstrap-era security guidance with an operational supported-version, private-reporting, response, disclosure, and trusted-publishing policy.
- Removed the confirmed-invalid npm registry `_authToken` entry from the maintainer's user configuration. `~/.npmrc` is now absent.
- Re-ran repository privacy, dependency, package, and remote residual-risk checks without publishing or mutating GitHub settings.

### Changed Files

- `.github/workflows/publish.yml`
- `.github/workflows/verify.yml`
- `.gitignore`
- `SECURITY.md`
- `docs/superpowers/specs/2026-07-20-security-hardening-design.md`
- `docs/superpowers/plans/2026-07-20-security-hardening.md`
- `reports/2026-07-20.md`

### Verification

- PASS: GitHub API resolved `actions/checkout@v6` to `df4cb1c069e1874edd31b4311f1884172cec0e10` and `actions/setup-node@v6` to `249970729cb0ef3589644e2896645e5dc5ba9c38`.
- PASS: no mutable `actions/checkout@v*` or `actions/setup-node@v*` reference remains in either workflow; each approved SHA appears twice.
- PASS: Ruby Psych parsed `.github/workflows/verify.yml` and `.github/workflows/publish.yml`.
- PASS: Git ignores `.env`, `.env.local`, and `.npmrc`; `.env.example` remains trackable.
- PASS: the user-level npm registry token key is absent and the previous `~/.npmrc` file no longer exists.
- PASS: tracked files and all `47` then-reachable commits produced zero findings for known personal identity, unexpected email, local absolute path, token-format, private-key, and long secret-assignment patterns. All reachable commit author and committer identities use `kim1124` with a GitHub noreply address.
- PASS: `npm audit --json` reported `0` vulnerabilities across `157` dependencies at every severity.
- PASS: `npm run verify` completed with `13` Vitest files, `105` tests, TypeScript checks, and the production build.
- PASS: `npm --cache /tmp/comins-table-security-npm-cache pack --dry-run --json` reported `25` package files and no `.npmrc`, `.env*`, workflow, report, or source file. The package includes `9` source maps.
- PASS: `git diff --check` passed for the repository security changes.

### Read-Only Remote Findings

- GitHub Actions is enabled with `allowed_actions=all`; repository-wide SHA pinning enforcement is disabled.
- The repository has `0` rulesets, no classic `main` branch protection, and CodeQL default setup is not configured.
- Open GitHub secret-scanning alerts: `0`. Open Dependabot alerts: `0`.
- The public GitHub profile exposes no email. A public display name exists, but its value was not emitted during this audit.
- Three known pre-rewrite commit objects remain directly accessible: `966a9ed`, `780366b`, and `9cd2719`. The previously reachable `1a17905b7f13f5782dc26f67968427fdc7ef3db4` object is no longer accessible through the commit API.
- Public npm metadata for `comins-table@0.1.0` still has a pre-rewrite `gitHead`, non-noreply publisher and maintainer email metadata, and no provenance attestation.

### Residual Risks and Priority

1. **High — provider-retained personal metadata:** npm `0.1.0` still exposes publisher and maintainer email metadata, and three known GitHub pre-rewrite objects remain accessible. Continue the existing npm Support case and file or continue a GitHub privacy/cache-removal request. Do not republish or create a duplicate npm release as a workaround.
2. **High — unprotected default branch:** `main` has no ruleset or classic protection, so force-push and deletion prevention are not enforced remotely. Add a minimal ruleset before considering stricter pull-request and status-check requirements.
3. **Medium — incomplete Actions policy:** the committed workflows are SHA-pinned, but GitHub still permits all actions and does not enforce SHA pinning repository-wide. Restrict allowed actions and enable SHA pinning after the local commits are pushed and the workflow SHAs are confirmed remotely.
4. **Medium — no CodeQL analysis:** dependency and secret alerts are clear, but no CodeQL default setup exists for JavaScript/TypeScript source analysis.
5. **Medium — account controls unverified:** GitHub account 2FA/recovery-code state and current npm account 2FA/trusted-publisher state could not be revalidated from the available authenticated CLIs after the stale npm token was removed. Verify these through the provider account pages.
6. **Low — historical local retention:** local reflogs may still retain pre-rewrite identity metadata. Local destructive garbage collection remains excluded because local retention was accepted and rollback/history destruction was not requested.
7. **Informational — published source maps:** the package intentionally includes `9` source maps. This is not a secret finding for the open-source repository, but it remains part of the public artifact surface.

### External Actions Not Performed

- No remote push, GitHub repository setting mutation, history rewrite, package publication, tag, GitHub Release, or duplicate support request was performed.

## Grid Layout Security Parity Rollout

### Work Time

- `2026-07-20 18:18-18:33 KST`

### Summary

- Created the active remote `main-protection` ruleset before pushing changes, matching the `comins-layout` PR, `verify`, deletion, and non-fast-forward controls.
- Split staged publishing into an unprivileged source verification/package job and a minimal `npm` environment/OIDC job that receives only the verified tarball.
- Disabled persisted Git checkout credentials and pinned all four workflow actions to official full commit SHAs.
- Added weekly review-only Dependabot update groups for npm and GitHub Actions.
- Extended local credential and private-key ignore rules while preserving sanitized example files.
- Prepared `comins-table@0.1.1` as an unpublished privacy-safe metadata release candidate with no runtime or public API change.
- Confirmed that the Grid Layout widget-selector fix has no Table equivalent because Table does not interpolate consumer row, tree, or column IDs into DOM selector strings.

### Changed Files

- `.github/dependabot.yml`
- `.github/workflows/publish.yml`
- `.github/workflows/verify.yml`
- `.gitignore`
- `CHANGELOG.md`
- `SECURITY.md`
- `package.json`
- `package-lock.json`
- `docs/superpowers/specs/2026-07-20-security-parity-rollout-design.md`
- `docs/superpowers/plans/2026-07-20-security-parity-rollout.md`
- `reports/2026-07-20.md`

### GitHub Ruleset

- PASS: created ruleset `main-protection` with ID `19199868`, enforcement `active`, target `branch`, and scope `~DEFAULT_BRANCH`.
- PASS: the ruleset has no bypass actors.
- PASS: pull requests are required with zero approving reviews; the `verify` check uses GitHub Actions integration ID `15368` and strict branch freshness.
- PASS: default-branch deletion and non-fast-forward updates are blocked.

### Local Verification

- PASS: GitHub API resolved the official action refs to `checkout=df4cb1c069e1874edd31b4311f1884172cec0e10`, `setup-node=249970729cb0ef3589644e2896645e5dc5ba9c38`, `upload-artifact=ea165f8d65b6e75b540449e92b4886f43607fa02`, and `download-artifact=d3f86a106a0bac45b974a628896c90dbdf5c8093`.
- PASS: Ruby Psych parsed both workflows and `.github/dependabot.yml`.
- PASS: all `7` workflow `uses:` references use approved full SHAs; both checkouts set `persist-credentials: false`.
- PASS: only `stage` has `environment: npm` and `id-token: write`; it contains no checkout, project install, build, test, or consumer command.
- PASS: package and lockfile root versions are exactly `0.1.1`; their diff changes only the three root version fields.
- ENVIRONMENT: the first browser run passed TypeScript, `13` Vitest files, `105` tests, and build, then failed because the sandbox denied `127.0.0.1:4002`. The command also forwarded `--workers` incorrectly and emitted an npm configuration warning; that invocation is not counted as a passing full gate.
- PASS: the clean rerun of `npm run verify:full` with local binding allowed completed TypeScript, `13` Vitest files, `105` tests, production build, and `81` Playwright tests.
- PASS: `npm run test:consumer` installed the local candidate with `7` packages and imported the public package contract.
- PASS: `npm --cache /tmp/comins-table-parity-npm-cache pack --dry-run --json` reported `25` files for `0.1.1`, with no environment, npm credential, workflow, report, planning, or source file; `9` public source maps remain.
- PASS: `npm audit --json` and `npm audit --omit=dev --json` both reported zero vulnerabilities across `157` resolved dependencies and zero production vulnerabilities.
- PASS: workspace, package tarball, and all `50` then-reachable commits produced zero findings for known personal identity, unexpected email, identifying local path, private-key, provider-token, npm-token, and long secret-assignment patterns. Reachable commit metadata uses the GitHub handle and noreply addresses.
- PASS: `git diff --check` and workflow boundary checks passed.

### Remaining Rollout Steps

- Push `codex/security-hardening`, create the protected PR, wait for `verify`, and merge to `main`.
- After merge, restrict Actions to GitHub-owned full-SHA actions, enable CodeQL default setup, observe its exact successful check, and add that check to `main-protection`.
- Enable and verify Dependabot malware alerts through the repository security UI if available.
- Re-run remote privacy and npm-state checks and record the remote closure separately.

### Publication Boundary

- `comins-table@0.1.1` was not staged or published. No dist-tag, npm version, Git tag, GitHub Release, or old-version state was changed.
- npm account email, 2FA, publishing access, and trusted-publisher identity remain prerequisites for a separately approved release.

## Remote Security Closure

### Work Time

- `2026-07-20 18:34-18:50 KST`

### Protected Merge

- PASS: ready PR `#1` passed the required `verify` job in `2m17s` and merged through `main-protection` without a bypass.
- PASS: merge commit `d5f19ff88e7e698cac9be86dff83f54420f61fb3` is present on remote `main`.
- PASS: the merged release candidate remains `0.1.1`; no npm stage, publish, dist-tag, Git tag, or GitHub Release operation occurred.

### GitHub Security Controls

- PASS: Actions is enabled with `allowed_actions=selected` and repository-wide `sha_pinning_required=true`.
- PASS: selected-actions permits GitHub-owned actions only; verified third-party actions are disabled and `patterns_allowed` is empty.
- PASS: CodeQL default setup is configured for JavaScript/TypeScript with the default query suite and weekly schedule.
- PASS: the first CodeQL analysis completed on remote `main` with `87` rules, `0` results, no error, and no warning.
- PASS: `main-protection` remains active with no bypass actors, strict branch freshness, pull-request, deletion, and non-fast-forward controls. Required checks are exactly `verify` and `Analyze (javascript-typescript)`, both bound to GitHub Actions integration ID `15368`.
- PASS: the authenticated Advanced Security UI saved Dependabot malware alerts successfully and exposed the enabled-state `Disable malware alerts` control afterward.
- PASS: private vulnerability reporting, Dependabot alerts and security updates, secret scanning, and push protection are enabled. Open CodeQL, secret-scanning, and Dependabot alert counts are all `0`.
- PASS: the `npm` environment requires the configured maintainer review and accepts deployments only from branch `main`.

### Final Privacy and Registry Audit

- PASS: the public GitHub profile exposes no email, uses the expected handle, and its display name is absent or differs from the known local real-name identity; no profile value was printed during verification.
- PASS: all `53` commits reachable from the merged `main` produced zero unexpected-email, identifying local-path, private-key/provider-token, and long secret-assignment matches. The merge commit uses the public display name with a GitHub noreply author address and the GitHub service committer; it does not expose the local real name or a personal email.
- PASS: `npm view comins-table versions dist-tags --json` still reports only public version `0.1.0` and `latest=0.1.0`; candidate `0.1.1` is not public.
- CONFIRMED RISK: the official npm registry response for `0.1.0` still contains two public identity-email fields, at least one privacy-unsafe address, no provenance attestation, and a `gitHead` that does not match the rewritten remote `main`.
- CONFIRMED RISK: known pre-rewrite GitHub commit objects `966a9ed`, `780366b`, and `9cd2719` remain directly accessible. Object `1a17905` remains inaccessible.

### Residual Risks and Next Approval Gates

1. **High — provider-retained personal metadata:** continue the existing npm Support request and GitHub privacy/cache-removal request for the old npm identity fields and three accessible pre-rewrite commit objects. A replacement publication does not remove these records.
2. **High — release account gate:** before any separately approved `0.1.1` release, revalidate the npm account's verified privacy-safe email, 2FA, publishing access, trusted-publisher repository/workflow/environment identity, staged tarball metadata, and provenance.
3. **Medium — account recovery controls:** GitHub account 2FA and recovery-code storage remain outside repository-level verification.
4. **Low — local retention:** local reflogs may retain pre-rewrite identity metadata; destructive local garbage collection remains intentionally excluded.
5. **Informational — public source maps:** the candidate intentionally contains `9` source maps as part of the open-source package surface.

## Repository Hygiene and Local Benchmark Boundary

### Work Time

- `2026-07-20 21:24-22:23 KST`

### Sensitive Information Audit

- PASS: the baseline tracked tree contained `170` files and produced zero findings for personal email, real-name markers, identifying home paths, provider-token formats, API-key or long secret assignments, private-key headers, JWTs, and credential-bearing URLs.
- PASS: all `60` commits and `696` unique blobs reachable from the current `origin/*` references produced zero high-confidence sensitive-content findings; reachable author and committer metadata uses public GitHub identities and noreply or platform addresses only.
- PASS: the npm package candidate contains `25` files and no environment file, credential file, local-only tree, repository hook, test, report, or repository utility.
- PASS: open GitHub secret-scanning, Dependabot, and CodeQL alert counts remain `0`; secret scanning and push protection remain enabled.
- CONFIRMED PROVIDER METADATA: the public npm `0.1.1` registry record exposes two unique publisher, approver, or maintainer email values. No value was printed or copied into the repository. At least one uses a consumer-mailbox domain, so the maintainer must keep both values non-identifying aliases.
- LOCAL-ONLY: disconnected Git objects and reflogs may retain old identity metadata, but they are outside current remote references and remain intentionally local. No provider-specific token format was detected in that local-only object set.

### Preventive Controls

- Added a value-redacting repository hygiene scanner for the tracked working tree, complete staged index, and reachable commit and blob history.
- The scanner rejects personal email addresses, non-noreply Git author email, non-public local Git author, commit, and annotated-tag names, optional ignored local identity markers, sensitive filenames, identifying home paths, high-confidence provider credentials, private keys, JWTs, credential URLs, long secret assignments, forced tracked credential or `.local/` paths, named third-party table comparison references and research paths, unsafe commit and annotated-tag messages, nested tag-object metadata, and sensitive content retained only in intermediate commits.
- Verification and publication checkouts now fetch complete reachable history so CI cannot miss unsafe branch commit metadata through a shallow clone.
- Added `npm run check:hygiene` to the mandatory `npm run verify` path used by GitHub Actions.
- Added opt-in repository pre-commit and pre-push hooks through `npm run setup:hooks`; the current checkout has both hooks enabled. Pre-push checks the exact SHA supplied by Git, its tip tree, and every changed file between the remote base and pushed tip rather than assuming the current branch.
- Expanded ignored credential material to include common network, package-manager, service-account, Java keystore, and local secret paths.

### Local Benchmark Classification

- Detailed third-party comparison research is absent from the current tracked tree and npm package.
- Removed named third-party products from the public README boundary statement.
- Created the ignored `.local/benchmarks/` taxonomy for source catalogs, vendor notes, feature and UX comparisons, raw and processed measurements, samples, assets, license notes, decisions, and split-source archives.
- Historical comparison documents remain reachable through old remote commits even though they were deleted from the current tree. Removing those historical copies requires a separately approved full-history rewrite and coordinated cleanup of every affected remote branch.

### Changed Files

- `.gitignore`
- `.githooks/pre-commit`
- `.githooks/pre-push`
- `.github/workflows/publish.yml`
- `.github/workflows/verify.yml`
- `AGENTS.md`
- `README.md`
- `package.json`
- `scripts/check-repository-hygiene.mjs`
- `scripts/consumer-smoke.mjs`
- `test/repository-hygiene.test.ts`
- `reports/2026-07-20.md`
- ignored local `.local/benchmarks/` and `.local/private/` material

### Verification

- PASS: hygiene scanner TDD gate completed with `31` focused tests covering clean content, the complete staged index, forced tracked local-only and credential paths, redacted content and filenames, UTF-16 content, configured and overridden Git identity, optional local blocklist markers, reachable commit and annotated-tag metadata and messages, nested and lightweight tags, intermediate history blobs and comparison files, exact pre-push refs and tip trees, deletion-only cleanup commits, token and private-key formats, the complete local benchmark catalog matcher set, named comparison material and research paths, and safe matcher self-inspection.
- PASS: `npm run check:hygiene` passed against the tracked working tree and scanned `991` reachable blob and tree objects from `HEAD`.
- PASS: focused public documentation tests, the full repository verification gate, consumer package smoke test, dry-run package inspection, staged-snapshot hygiene check, and `git diff --check` completed successfully.
- ENVIRONMENT: the first sandboxed `npm audit --json` attempt could not resolve the npm registry. The network-enabled rerun completed with `0` vulnerabilities across `157` resolved dependencies.

### Residual Risks

1. **Medium — npm provider identity fields:** repository controls cannot remove generated public publisher, approver, or maintainer metadata. Keep the npm account email as a non-identifying alias and re-check every release record.
2. **Medium — historical benchmark copies:** deleted comparison research remains remotely reachable by commit SHA until an explicitly approved destructive history rewrite is completed.
3. **Low — pattern-based detection:** the scanner covers high-confidence identifiers and credentials but cannot prove that every arbitrary secret format or contextual identity clue is absent. GitHub secret scanning and human review remain required.
4. **Low — per-clone hook installation:** the pre-commit and pre-push hooks are active in this checkout only. Every new clone must run `npm run setup:hooks`; CI enforcement through `npm run verify` is the authoritative shared gate.

### External Boundary

- Work was isolated from the earlier local branch containing private operational closure notes. No remote push, history rewrite, provider mutation, publication, tag, or GitHub Release was performed.
