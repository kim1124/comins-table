# 2026-07-14 Worklog

## 16:16 KST - Merge `dev-codex` into `main`

### Summary

- Fetched the latest remote refs and created local `main` from `origin/main`.
- Merged local `dev-codex` into `main` with unrelated-history support.
- Preserved the `dev-codex` public product documentation when resolving the `README.md` add/add conflict.
- Preserved the existing untracked screenshots under `reports/artifacts/`.

### Changed Files

- Merge commit: `fc7dff4 Merge branch 'dev-codex'`
- Added this worklog: `reports/2026-07-14.md`

### Verification

- PASS before merge: `npm run verify` - TypeScript lint, 10 Vitest files / 90 tests, production build.
- PASS before merge: `npm run test:e2e -- --workers=1` - 80 tests.
- PASS after merge: `git diff --exit-code dev-codex main` - no tree differences immediately after the merge.
- PASS after merge: `git diff --check HEAD^1..HEAD`.
- PASS after merge: `npm run verify` - TypeScript lint, 10 Vitest files / 90 tests, production build.
- PASS after merge: `npm run test:e2e -- --workers=1` - 80 tests.

### Result

- Local `main` contains the complete local `dev-codex` tree through merge commit `fc7dff4`.
- The local `dev-codex` branch remains available and was not deleted.

### Residual Risks and Follow-up

- Local `main` has not been pushed to `origin/main` because remote publication was not requested.
- Local `dev-codex` and `origin/dev-codex` have unrelated/divergent histories; this merge used the explicitly identified local `dev-codex` source.
- Two Playwright-generated screenshots under `reports/artifacts/visual-typography/` remain untracked and preserved.

## 16:24 KST - Document standalone repository ownership

### Summary

- Defined this repository as the source of truth for Comins Table implementation, Playground, documentation, tests, and releases.
- Clarified that development and verification commands run from this repository root without an `npm --workspace` prefix.
- Corrected the Korean Playground startup command from a workspace command to `npm run dev`.

### Changed Files

- `README.md`
- `docs/user/12-playground.md`
- `docs/ko/12-playground.md`

### Verification

- PASS: `npm run verify` - TypeScript lint, 10 Vitest files / 90 tests, production build.
- PASS: checked the documentation for `npm --workspace=<package> run` commands - no legacy workspace command remains.

### Residual Risks

- The existing Playwright-generated screenshots under `reports/artifacts/visual-typography/` remain untracked and preserved.

## 16:54 KST - Summary Row and Tree Grid design approval record

### Summary

- Recorded the approved delivery order: operational quality gates, Summary Row, Tree Grid, then Playground and public documentation.
- Fixed the Tree Grid controlled data contract as `item`, `expand`, and recursive `children` nodes while keeping existing column definitions bound to `item`.
- Added the requested Playground `Tree Grid` route and English/Korean user-documentation scope to the design.
- Kept `0.1.0` publication outside the scope until the user explicitly orders a release after Tree Grid is complete.

### Changed Files

- `docs/superpowers/specs/2026-07-14-summary-row-and-tree-grid-design.md`
- `reports/2026-07-14.md`

### Verification

- Pending design-review approval. No production source, package configuration, or test behavior was changed in this step.

### Residual Risks

- The implementation plan and production changes are intentionally deferred until this written design is reviewed.
- The existing Playwright-generated screenshots under `reports/artifacts/visual-typography/` remain untracked and preserved.

## 17:00 KST - Tree, Row Expand, and Row Grouping compatibility research

### Sources Checked

- Repository source: `src/core.ts`, `src/index.tsx`, `styles.css`, `src/component-renderer.tsx`
- Repository tests and examples: flat row movement, selection, clipboard, pagination, lazy loading, infinite scrolling, and virtualization coverage
- Approved Tree Grid design: `docs/superpowers/specs/2026-07-14-summary-row-and-tree-grid-design.md`
- No external documentation or web source was required because the behavior under review is determined by this repository's current implementation.

### Confirmed Findings

- Tree Node expansion controls which nested data nodes are included in the visible pre-order row list; it changes hierarchy-aware sorting and prevents Tree V1 row movement, pagination, lazy loading, and infinite scrolling.
- Current row operations use a flat `state.rows` and fixed `rowHeight` virtual-window calculation. Flat row movement moves one data row, and clipboard/range selection indexes flat visible rows.
- Row Expand is therefore a separate detail-rendering feature: its detail must follow the owner row while normal flat sorting, pagination, infinite scrolling, lazy loading, and row movement continue to target only the owner row.
- Dynamic Row Expand content cannot reuse the Tree V1 fixed-height virtual calculation. It will require a separate variable-height virtual-layout design when that feature is scheduled.
- Row Grouping is a derived projection of flat rows and should retain separate group ids and expansion state rather than reuse `CominsTreeNode`.

### Decision Applied To Tree Scope

- Tree Grid implementation may proceed without a functional conflict, provided it keeps Tree node expansion, flat Row Expand state, and future grouping state separate.
- The Tree design now documents these boundaries and limits shared implementation to an accessible disclosure-button primitive.

### Residual Risks

- Row Expand and Row Grouping APIs are not designed or implemented in the Tree delivery; their future variable-height virtualization and group-operation semantics require dedicated design work.
- The existing Playwright-generated screenshots under `reports/artifacts/visual-typography/` remain untracked and preserved.

## 17:18 KST onward - Summary Row, Tree Grid, Playground, and verification gates

### Summary

- Added the controlled Summary Row API with `count`, `sum`, `avg`, `min`, and `max` aggregators. It uses all controlled flat rows before pagination; Tree Grid uses leaf `item` rows only.
- Added controlled Tree Grid with `{ item, expand?, children? }` nodes, immutable expand/item updates, global id validation, recursive sibling sorting, accessible expanders, fixed-row-height virtualization, and existing `item`-based columns.
- Added the `/examples/tree-grid` Playground page, Sidebar entry, runnable code sample, Tree Grid V1 constraints, English/Korean public guides, and README scope updates.
- Preserved the boundary confirmed in research: Tree expand controls nested hierarchy visibility only. Future flat Row Expand and Row Grouping retain separate state and require their own variable-height/projection designs.
- Added package quality gates: GitHub Actions verification, a packed-consumer smoke test, and ignored generated report artifacts. Corrected stale Advanced Feature Playground wording.
- Stabilized the DevTools counter tests around the actual SPA navigation path and representative virtual-scroll baseline. The test continues to enforce bounded rendered rows, DOM nodes, heap, listeners, and no document-count increase after returning to the basic page.

### Changed Files

- Library: `src/summary.ts`, `src/tree.ts`, `src/index.tsx`, `styles.css`
- Library tests: `test/summary-core.test.ts`, `test/tree-core.test.ts`, `test/tree-table.test.tsx`, `test/table-interaction.test.tsx`, `test/public-api.test.tsx`
- Playground/docs: `example/src/features/TreeGridFeature.tsx`, feature registry/types, Docs routes/code samples/option guide, `README.md`, `docs/user/12-playground.md`, `docs/user/17-tree-grid.md`, `docs/ko/12-playground.md`, `docs/ko/17-tree-grid.md`
- Browser/quality gates: `test/playwright/specs/tree-grid.spec.ts`, Playground docs specs, `test/playwright/specs/virtualization.spec.ts`, `.github/workflows/verify.yml`, `scripts/consumer-smoke.mjs`, `package.json`, `.gitignore`
- Planning: `docs/superpowers/plans/2026-07-14-summary-row-and-tree-grid.md`

### Verification

- PASS: focused Summary/Tree Vitest and public API tests during test-first implementation.
- PASS: `npm run verify` - TypeScript lint, 13 Vitest files / 105 tests, production build.
- PASS: focused Tree Playground/docs E2E - 13 Playwright tests.
- PASS: `npm run test:e2e -- --workers=1 --reporter=dot` - 81 non-performance Playwright tests.
- PASS: `npm run test:perf -- --workers=1 --reporter=dot` - 23 DevTools/CDP performance tests.
- PASS: `npm run test:consumer` - built tarball installed into a temporary React consumer and root/core/clipboard/selection/CSS entries resolved.
- PASS: `npm pack --dry-run --json` - package includes `dist/summary.*` and `dist/tree.*`.
- PASS: `git diff --check` before final documentation/report staging.

### Verification Environment Notes

- Browser commands initially failed in the default sandbox because `127.0.0.1:4002` could not bind (`listen EPERM`). The Vite server was started and browser gates were rerun in an allowed environment.
- A first performance run exposed test-contract issues rather than a Tree Grid regression: full-document test navigation retained an earlier Chromium document counter, and immediate post-dispatch listener sampling was timing-sensitive. The test now follows SPA return navigation and samples after representative scroll/effect settlement; the final full performance gate passed.
- Independent code review found and the final pass fixed two Tree V1 blockers: flat row-level copy/paste could lose the inserted node or overwrite a visible item, and JavaScript/`any` callers could activate remote loading despite the TypeScript restrictions. Tree now blocks row-level clipboard and hard-disables remote-loading props at runtime; focused regression, public API, documentation, full E2E, and full performance gates were rerun afterward.

### Residual Risks and Follow-up

- Tree Grid V1 intentionally excludes pagination, lazy loading, infinite scrolling, row drag, row-level copy/paste, remote child loading, and variable-height rows.
- Flat Row Expand and Row Grouping remain future work. They must preserve flat sorting/pagination/loading/movement semantics and require a separate variable-height virtual-layout/projection design.
- No manual headed Chrome DevTools Performance Monitor session was run in this change. CDP-based `Memory.getDOMCounters` and `Performance.getMetrics` gates passed, but manual interactive confirmation remains a release-preparation follow-up.
- `comins-table@0.1.0` was not published. Publication remains an explicit user-commanded operation after feature/release review.

## 17:56 KST - Standalone instruction surface

### Summary

- Added a repository-local `AGENTS.md` that defines Comins Table as a standalone source of truth and prevents KMSF workspace commands from being used for normal work.
- Added risk-based package verification rules, including focused documentation, Playground, performance, consumer, and release checks.
- Updated global Codex guidance and the user-managed research, planning, development-brief, and Playwright skills to use material-decision and risk-based conditions rather than unconditional question, report, or full-gate loops.

### Changed Files

- `AGENTS.md`
- `$CODEX_HOME/AGENTS.md`
- `$CODEX_HOME/skills/ask-research/SKILL.md`
- `$CODEX_HOME/skills/ask-plan/SKILL.md`
- `$CODEX_HOME/skills/dev-brief/SKILL.md`
- `$CODEX_HOME/skills/playwright/SKILL.md`
- `reports/2026-07-14.md`

### Verification

- PASS: `npm run test:run -- test/user-docs.test.ts` - 1 Vitest file / 6 tests.
- PASS: `git diff --check` after repository documentation changes.
- PASS: Markdown fence check for the project and global instruction files.
- PASS: `codex --strict-config --version` - Codex CLI 0.144.4 accepted the updated global configuration. The sandbox could not create PATH aliases, which does not affect config parsing.
- PASS: standalone/KMSF residual scan - active instructions retain only explicit KMSF-history and conditional KMSF-root references; no inherited workspace command is present.
- PASS: skill behavior scenarios - clear one-line documentation edits avoid the full Brief; unresolved material plan priority is asked once without automatic research-file creation; manual browser inspection does not replace package E2E; package E2E/performance requests use the package npm commands.

### Residual Risks

- The global and user-skill edits apply to future Codex tasks and may require a new task or application restart before every surface reloads them.
- `comins-table@0.1.0` publication remains an explicit user-commanded operation.
