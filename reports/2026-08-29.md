# 2026-08-29 전체 기능 및 예제 테스트 신뢰성 재검토

- 작업 일시: 2026-08-29 00:37 KST
- 작업 브랜치: `codex-0-1-8-transfer-pinning-2`
- 기준 커밋: `57b33096241eee5e5f610d82875a9e7d5ea96493`
- 대상 버전: `comins-table@0.1.8`
- 상태: 로컬 감사·보강·전체 검증 완료, PR #54 GitHub-hosted 3회 실패 원인 교정 commit 재검증 대기

## 결론

- Playground의 공개 Feature 25개 모두에 canonical route smoke와 기능 전용 browser spec 소유권이 존재한다.
- 일반 Chromium E2E는 32개 spec의 155개 테스트, performance E2E는 10개 spec의 28개 테스트다. 25개 canonical route smoke는 route별 독립 테스트로 격리했다.
- 최종 로컬 결과는 Unit 374/374, 일반 E2E 155/155, performance 28/28이며 TypeScript, production build, hygiene, security와 license gate도 통과했다. hosted 3차 교정 후 집중 검증은 Column Pinning 25/25와 Playground route·docs 29/29를 통과했다.
- 정상 동작을 실패로 보고하던 CI scope/readme 계약과 physical-scrollbar memory 계약을 실제 실행 모델에 맞게 교정했다. 제품 assertion이나 성능 threshold는 완화하지 않았다.
- 로컬 Playwright는 항상 single worker, local retry 0, `failOnFlakyTests: true`, `forbidOnly: true`로 실행되며 `verify:full`은 fast gate → 일반 E2E → performance 순서를 포함한다.

## 공개 기능별 Browser 소유권

| Feature | Route | 전용 검증 |
| --- | --- | --- |
| Basic | `/docs/getting-started` | 기본 렌더, 높이, clipboard/range interaction |
| CRUD | `/examples/crud` | add/update/delete/reset controlled data |
| Size | `/examples/size` | manual 300px, parent 500px sizing |
| Theme | `/examples/theme` | 6개 theme 전환, DOM 수와 mount 유지 |
| Loading | `/examples/loading` | initial/refetch/empty/ready와 stale response |
| Header | `/examples/header` | resize, move, sort, visibility, layout, cancel/animation |
| Header Groups | `/examples/column-groups` | 2-depth parent/child resize·move·visibility |
| Column Pinning | `/examples/column-pinning` | left/right pin, resize, responsive demotion, summary/bottom rail sync |
| Cell | `/examples/cell` | format/style/event/resize/selection option |
| Selection & Clipboard | `/examples/selection-clipboard` | row/cell/range selection과 keyboard paste |
| Component | `/examples/component` | built-in/custom renderer, editor, menu, virtual list |
| Row | `/examples/row` | event/style/disabled/drag/placeholder |
| Row Expand | `/examples/row-expand` | disclosure, focus, fixed/auto detail, scroll/resize anchor |
| Row Grouping | `/examples/row-grouping` | explicit model, sort, disclosure, Row/Group Drag와 CRUD |
| Cross-Table Drag | `/examples/cross-table-drag` | Row/Group transfer, duplicate policy, auto-scroll, empty Body drop |
| Column Filtering | `/examples/column-filtering` | controlled editor, Group projection, large projection bound |
| Summary Row | `/examples/summary-row` | aggregate, `colSpan`, formatter와 style |
| Tree Grid | `/examples/tree-grid` | expand/fold, ref, style/component, 10,000-node bound |
| Context Menu | `/examples/context-menu` | zero/single/multiple selection과 Row/Cell data |
| Export | `/examples/export` | CSV/JSON output 전환 |
| Ref API | `/api/ref` | selection, sort, layout save/restore, Row move |
| Pagination | `/performance/pagination` | first/previous/next/last와 page Row projection |
| Infinite Scroll | `/performance/infinite-scroll` | append once, exhaustion, refresh, request offsets |
| Lazy Load | `/performance/lazy-load` | initial/append/refresh와 stale response |
| Virtualization | `/performance/virtualization` | 100,000 Row DOM bound, sticky/resize/scroll sync, memory/latency |

`example/src/docs/featureRouteManifest.ts`를 runtime route/feature registry와 Unit 계약으로 연결하고, `user-playground-docs.spec.ts`가 위 25개 route를 모두 실제 렌더링한다. `test/test-reliability-contract.test.ts`는 각 Feature의 전용 spec 파일 존재를 추가로 검증한다.

## 발견 및 교정 사항

1. 전체 route smoke가 과거 22개 hard-coded 목록에 머물러 Row Expand, Column Pinning, Cross-Table Drag를 누락했다. 25개 canonical manifest 기반으로 교체했다.
2. package public export인 root `styles.css` 단독 변경이 기존 inline CI classifier에서 Browser E2E를 실행하지 않았다. pure classifier와 Unit 계약을 추가하고 workflow가 해당 classifier를 사용하도록 변경했다.
3. `forbidOnly`와 flaky retry 실패 처리가 CI에서만 강제됐고 local `verify:full`은 performance를 포함하지 않았다. local/CI 공통 fail-closed 설정과 명시적 `verify:local`, `verify:full` 순서로 교정했다.
4. 일반 Row event E2E에 350ms 고정 대기가 있었다. 제거 후 해당 spec 25건을 `repeat-each=5`, single worker로 실행해 모두 통과했다.
5. README GIF workflow 테스트가 제거된 inline regex 문자열을 계속 요구해 올바른 classifier 구현을 실패로 보고했다. 실행 가능한 `classifyVerificationScope()` 결과를 직접 검증하도록 교정했다.
6. physical-scrollbar memory test가 `page.goto()`로 새 문서를 반복 생성하고 CDP `Memory.getDOMCounters`의 BFCache/retained document node를 제품 누수처럼 비교했다. 최초 진입만 document navigation으로 두고 fixture 진입과 복귀를 동일 문서 SPA lifecycle로 바꿨다. 제품 threshold는 유지한 상태로 통과했다.
7. 전체 memory audit도 기본 화면 복귀에 `page.goto()`를 사용하고 Header Drag 전에 1,100ms 고정 대기를 사용했다. 실제 sidebar navigation과 `data-column-placeholder` 상태 확인으로 교체했으며 6/6 통과했다.
8. classic vertical scrollbar gutter가 있는 hosted Chromium에서 Body의 실제 입력 폭은 외곽 폭을 기준으로 제한되지만 bottom rail은 Body의 `clientWidth`로 렌더링되어 native horizontal range가 15px 더 길었다. `scrollbar-gutter: stable both-edges`에서는 차이가 30px로 확대됐다. Header·Summary의 pinned 정렬 폭은 유지하고 bottom rail만 Body 외곽 폭에 맞춰 두 입력 surface의 native 끝점을 일치시켰다. 첫 hosted 실패 뒤 적용한 flex cross-axis stretch 차단 가설과 두 번째 실패 뒤 적용한 테스트-only native clamp 가설은 후속 hosted 실행에서 각각 반증되어 폐기했다.
9. Cross-Table Drag는 Row가 있는 target만 검증하고 빈 Body 하단 영역 drop을 직접 검증하지 않았다. 실제 pointer hit-test와 transfer/focus 결과를 확인하는 browser case를 추가했다.
10. Playwright 실패 artifact 보존 기간을 3일에서 14일로 늘려 PR 실패 증거의 사후 분석 가능 기간을 확보했다.
11. 개별 performance 실행은 통과했지만 `verify:full` 순서에서 Component memory heap이 10% 기준을 약 0.5%p 초과하는 변동을 재현했다. 최초 Component 상호작용을 warm-up한 뒤 mount/unmount 3회를 측정하도록 변경해 일회성 V8 비용과 반복 누수를 분리했다. 동일 임계값으로 5/5 반복 통과했다.
12. Feature lifecycle memory는 baseline 1회와 측정 5회의 route workload가 비대칭이어서 10% 기준을 약 0.03%p 초과했다. warm-up과 측정에 동일한 5회 순환을 적용했으며 동일 임계값으로 5/5 반복 통과했다.
13. 100,000 Row virtual scroll memory는 route warm-up만 수행한 baseline과 최초 down/up workload 이후의 heap을 비교해 독립 반복 5회 중 1회를 10% 기준 약 0.28%p 초과로 보고했다. 동일한 down/up workload를 warm-up과 측정에 적용했으며 임계값을 유지한 상태로 5/5 반복 통과했다.
14. 두 번째 hosted 실행에서 Row Expand 정렬 검증이 첫 시도에만 실패하고 retry에서 통과했다. trace상 세 요소의 `boundingBox()`를 순차 호출하는 약 58ms 동안 레이아웃 프레임이 바뀌어 서로 다른 시점의 좌표를 비교했다. 한 번의 browser layout snapshot에서 세 `getBoundingClientRect()`를 함께 읽도록 교정했으며 기존 1px 정렬 기준을 유지한 상태로 5/5 반복 통과했다.
15. 세 번째 hosted 실행에서 25개 route를 한 테스트가 순차 방문하다 30초 test timeout을 소진했고 retry에서도 browser session이 닫혔다. timeout을 늘리지 않고 canonical manifest에서 route별 독립 테스트를 생성해 실패 격리와 route 식별성을 높였다. navigation mount/unmount 계약은 별도 테스트로 유지했으며 focused 29/29를 통과했다.

## 테스트 패턴 분류

- 일반 E2E에는 `waitForTimeout`이 0건이다. 고정 대기 8건은 GC/CDP metric 반영과 물리 scrollbar gesture를 다루는 performance harness 3개 파일에만 남아 있다.
- `test.skip`, `test.fixme`, `test.only`는 Playwright spec에 0건이며 Unit 계약이 재유입을 차단한다.
- programmatic `scrollTop`/`scrollLeft`와 synthetic `scroll`은 100,000 Row 위치 설정, remote load trigger, memory sampling처럼 큰 상태를 결정적으로 만드는 setup에 한정한다. Row/Group/Header Drag, resize, clipboard, context menu와 Column Pinning의 주 사용자 경로는 실제 mouse, keyboard, wheel 입력으로 검증한다.
- geometry assertion은 sticky 위치, resize clamp, drag target, overflow containment과 scrollbar boundary처럼 좌표가 제품 계약인 경우에 유지한다.
- CI required gate는 security, selected fast gate, selected Chromium E2E를 수행한다. 성능/CDP 계측은 로컬 `verify:full`에서 single worker로 수행하며 CI 일반 E2E와 섞지 않는다.

## 최종 검증

- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 374/374, production build 통과
- `npm run test:e2e -- --workers=1`: Chromium non-performance 155/155, retry 없이 통과
- Row/Cell 고정 대기 제거 반복: 25/25 (`repeat-each=5`, single worker) 통과
- 스크롤 변경 focused performance: 6/6 통과
- 동일 문서 memory audit: 6/6 통과
- 100,000 Row memory 대칭 workload 반복: 5/5 (`repeat-each=5`, single worker) 통과
- Column Pinning native 끝점·Row Expand 단일 프레임 좌표 반복: 15/15 (`repeat-each=5`, single worker) 통과
- Body 외곽 폭 기반 bottom rail·reserved gutter 반복: 25/25 (`repeat-each=5`, single worker) 통과
- route별 Playground smoke·docs focused: 29/29 (single worker) 통과
- `npm run test:perf -- --workers=1`: 최종 28/28 통과
- `npm run verify:full`: bottom rail 실제 range·route별 smoke 교정 후 hygiene/security/license/type/Unit/build → 일반 E2E 155/155 → performance 28/28 통합 순서로 최종 통과
- `git diff --check`: 통과

## 실패 분류

- 첫 focused performance 실행의 `listen EPERM 127.0.0.1:4002`는 Playwright webServer가 assertion 전에 차단된 sandbox 실행 환경 실패다. 별도 Linux/container 환경을 만들지 않고 동일 프로젝트 명령을 허용된 localhost 환경에서 재실행했다.
- 첫 전체 performance는 physical-scrollbar memory 1건이 `nodes 1,223 → 3,051`, `documents 1 → 2`로 실패했지만 현재 문서의 `liveElementCount`는 721로 동일했다. 동일 문서 SPA lifecycle로 테스트 계약을 교정한 최종 실행은 threshold 변경 없이 28/28 통과했다.
- 첫 전체 Unit은 README GIF test 1건이 삭제된 inline CI pattern을 요구해 373/374였다. pure classifier 실행 결과를 검증하도록 수정한 최종 Unit은 374/374다.
- 첫 `verify:full` 통합 실행은 Component memory heap 1건이 10% 기준을 약 0.5%p 초과했고 serial 후속 3건은 실행되지 않았다. 5회 독립 반복에서 4 pass/1 fail로 one-time heap baseline 변동을 확인했다. workload warm-up 후 mount/unmount 3회를 측정하는 강화된 계약은 5/5 통과했다.
- 다음 전체 memory audit은 Feature lifecycle의 1회 baseline 대비 5회 측정 workload 비대칭을 10% 기준 약 0.03%p 초과로 검출했다. 양쪽을 동일 5회 workload로 맞춘 강화된 계약은 5/5 통과했으며 최종 `verify:full`의 performance 28/28에도 포함됐다.
- 첫 GitHub-hosted 실행 `33220339054`는 Linux Chromium에서 Body `948/963`과 동기화 surface의 15px range 차이를 2건 검출했다. 최초에는 flex cross-axis stretch를 제품 원인으로 가정했지만, CSS 변경 후 두 번째 실행에서도 동일 수치가 재현되어 해당 가설을 기각했다.
- 두 번째 GitHub-hosted 실행 `33221557511`은 동일한 Column Pinning 2건과 retry에서만 통과한 Row Expand 1건을 검출했다. Column Pinning은 산술 끝점이 아닌 browser-clamped native 끝점을 입력 surface의 기준으로 사용하고 네 surface의 동기화 기준은 그대로 유지했다. Row Expand는 서로 다른 프레임의 좌표 비교를 단일 layout snapshot으로 교정했다. `failOnFlakyTests: true`가 retry 통과를 성공으로 은폐하지 않고 job을 실패 처리했다.
- 세 번째 GitHub-hosted 실행 `33222584548`은 bottom rail이 첫 예제에서 Body보다 15px, reserved-gutter 예제에서 30px 더 긴 native range를 가진 제품 문제 2건과 25-route 집계 smoke의 30초 timeout 1건을 검출했다. bottom rail을 Body 외곽 폭으로 맞추고 route smoke를 독립 테스트로 분할했으며 threshold나 timeout은 완화하지 않았다.
- CSS 수정 후 첫 `verify:full`은 100,000 Row memory heap이 10% 기준을 41,404 bytes 초과했고, 독립 5회 반복에서도 1회 재현됐다. DOM, document와 listener는 회수됐으며 baseline에 동일 down/up workload가 없던 비대칭을 교정한 뒤 5/5 반복과 최종 performance 28/28을 통과했다.

## 잔여 리스크

- 실제 browser 제품 검증은 로컬 Chromium 기준이다. Firefox, WebKit, 실제 Safari와 물리 mobile은 이번 범위에서 실행하지 않았다.
- CI change-scope classifier는 세 GitHub-hosted 실행에서 Chromium E2E를 정상 선택했고 flaky retry도 실패 처리했다. bottom rail 실제 range와 route별 smoke 교정 commit의 GitHub-hosted 재실행 결과는 PR #54 required check에서 별도 확인해야 한다.
- performance harness에 남은 고정 대기 8건은 OS scrollbar/CDP GC 계측 안정화 목적이다. 일반 사용자 동작 E2E와 분리되어 있으나 장기적으로 CDP event 또는 안정 상태 poll로 대체 가능한지 검토할 수 있다.
- PR #54는 열려 있다. merge, tag, GitHub Release와 npm publish는 수행하지 않았다.

## 추가 반영 - 0.1.8 Release Workflow 실행 경계 교정

- 작업 일시: 2026-08-29 11:28 KST
- 작업 브랜치: `codex-0-1-8-release-workflow`
- 기준 `main`: `7c3f21416e9fd78ef95dd18c97003ce92c2e345e`
- 실패 Stage 실행: `33226690104`

### 원인 및 결정

- Stage workflow의 보안, 라이선스, Unit, Build와 일반 Chromium E2E 155개는 통과했다.
- `verify:full`이 로컬 performance E2E 28개를 포함하도록 강화됐지만 기존 Stage workflow가 같은 명령을 계속 호출해 GitHub-hosted runner에서도 로컬 전용 성능 계측을 실행했다.
- hosted runner에서는 component interaction의 로컬 고정 시간 기준이 반복 실패했고, headed browser를 사용하는 physical scrollbar E2E 두 건은 XServer가 없어 실행할 수 없었다. Row Expand performance 한 건은 retry 통과하여 flaky fail-closed로 분류됐다.
- maintainer 결정에 따라 physical scrollbar와 performance/memory 계측은 로컬 Playwright E2E 전용으로 유지한다. GitHub CI와 Stage workflow는 portable headless 일반 E2E까지만 실행한다.

### 변경

- Stage workflow를 `npm run verify:full`에서 `npm run verify:local`로 변경했다.
- `verify:full`의 로컬 performance 28개, 기존 threshold와 assertion은 변경하지 않았다.
- reliability contract에 Stage workflow가 `verify:full` 또는 `test:perf`를 직접 실행하지 않는다는 회귀 검사를 추가했다.

### 잔여 단계

- reliability contract 6/6, security gate 21/21, repository hygiene와 `git diff --check`가 통과했다.
- 보호된 PR과 병합 후 검증이 필요하다.
- 수정 PR의 병합 후 같은 `0.1.8` Stage workflow를 다시 실행해야 한다.
- exact artifact 생성, Gitleaks, consumer smoke, npm environment 승인과 maintainer 2FA가 완료되기 전까지 `0.1.8`은 배포되지 않은 상태다.
- CI에서 안정적으로 실행 가능한 browser 검증과 로컬 E2E 전용 성능·OS 계측의 세부 분류는 별도 후속 검토 범위다.

## 추가 반영 - 로컬·GitHub 일반 E2E 실행 계약 일치

- 작업 일시: 2026-08-29 12:46 KST
- 작업 브랜치: `codex-e2e-contract-alignment`
- 기준 `main`: `a145624ca13f`
- 대상 버전: `comins-table@0.1.8`

### 결론 및 변경

- 로컬과 GitHub-hosted 일반 E2E가 Node 24.19.0, npm 11.17.0, Playwright Chromium headless, 1280x720 viewport, `en-US`, UTC, single worker와 retry 0 조건을 사용하도록 실행 계약을 일치시켰다.
- `.nvmrc`와 모든 `actions/setup-node`을 Node 24.19.0으로 고정했고, 해당 파일 변경도 fast 및 browser verification 범위로 분류한다.
- `PLAYWRIGHT_FAIL_ON_FLAKY_TESTS` workflow 환경변수에 의존하지 않고 Playwright 설정의 `failOnFlakyTests: true`와 retry 0을 공통 적용한다.
- 일반 E2E는 기본적으로 새 Vite server를 사용하며, 명시적으로 `PLAYWRIGHT_REUSE_EXISTING_SERVER=1`을 지정한 경우에만 기존 server를 재사용한다.
- classic scrollbar 환경에서 발생하는 `offsetWidth - clientWidth` inset을 horizontal content range에 반영해 Body와 하단 horizontal rail의 native 끝점을 일치시켰다.
- OS별 scrollbar 구현에 의존하던 E2E fixture는 24px transparent viewport inset으로 동일한 DOM 폭 조건을 결정적으로 재현하도록 교체했다. 실제 native scrollbar 폭을 특정 값으로 가정하지 않는다.

### 최종 검증

- Node 24.19.0 / npm 11.17.0에서 `npm ci` 완료
- `npm run verify`: hygiene, security 21/21, license 27/27, TypeScript, Vitest 377/377, production build 통과
- Column Pinning focused E2E: 5/5 통과
- `npm run verify:e2e`: 일반 Chromium E2E 155/155, single worker 및 retry 0으로 통과
- `git diff --check`: 통과

### 실패 분류 및 잔여 리스크

- 첫 전체 E2E의 `listen EPERM 127.0.0.1:4002`는 제한된 실행 영역이 fresh Vite server bind를 차단한 환경 실패다. 별도 Linux 또는 container를 구성하지 않고 동일 명령을 허용된 localhost 환경에서 재실행해 155/155를 확인했다.
- GitHub-hosted 결과는 local branch를 push하거나 PR을 생성하지 않았으므로 아직 미확인이다. 원격 쓰기 승인 후 동일 workflow 결과로 최종 대조해야 한다.
- performance, physical scrollbar, headed browser 검증은 기존 로컬 전용 경계를 유지하며 이번 변경에서 실행하지 않았다.
