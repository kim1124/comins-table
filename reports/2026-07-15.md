# 2026-07-15

## PVR Security Reporting Alignment

### Summary

- Added a repository-local security policy for GitHub Private Vulnerability Reporting.
- The maintainer confirmed Private Vulnerability Reporting is enabled for `kim1124/comins-table`.

### Changed Files

- `SECURITY.md`
- `reports/2026-07-15.md`

### Verification

- PASS: `git diff --check` after documentation changes.
- CONFIRMED BY MAINTAINER: GitHub Private Vulnerability Reporting is enabled.

### Residual Risks

- The GitHub PVR setting is maintained remotely and was not queried through the local GitHub CLI in this change.
- Supported versions and security-fix windows remain release-dependent until the first public release policy is published.

## Comins Contract v1.1 Adoption

### Summary

- Adopted the v1.1 shared contract in the repository AGENTS guidance.
- Declared the CSS side effect and replaced mutable development dependency tags with the existing lockfile-resolved version ranges.
- Documented the browser runtime boundary and the pre-release security prerequisites.

### Changed Files

- `AGENTS.md`
- `README.md`
- `SECURITY.md`
- `package.json`
- `package-lock.json`
- `reports/2026-07-15.md`

### Verification

- PASS: `npm run verify` (`13` test files, `105` tests, production build).
- PASS: `npm run test:run -- test/user-docs.test.ts` (`6` tests).
- PASS: `npm --cache /tmp/comins-table-npm-cache pack --dry-run --json` (`25` package files).
- PASS: package manifest and lockfile root match with no mutable dist-tags.
- PASS: `git diff --check`.

### Residual Risks

- GitHub dependency and secret-scanning alert settings remain remote configuration and were not queried or changed locally.
- SSR is documented as unsupported until a dedicated consumer integration contract is validated.
- The default npm cache has root-owned files, so package inspection used a temporary cache; this is an execution-environment issue, not a package failure.

## Codex Model Policy

### Summary

- Set `gpt-5.6-sol` with `xhigh` reasoning as the default for Comins work.
- Require at least `xhigh` for vulnerability investigation, runtime memory failures, and security work.
- Require at least `max` for instruction planning, Plan mode, and implementation-plan authoring or updates.

### Changed Files

- `AGENTS.md`
- `reports/2026-07-15.md`

### Verification

- PASS: global Codex configuration parsed with `codex --strict-config --version` (`0.144.4`).
- PASS: Comins root instruction policy consistency check and `git diff --check`.

### Residual Risks

- Model availability and supported reasoning levels remain dependent on the active Codex release and account access.

## Contract v1.1 Operational Closure

### Summary

- Documented the supported React, Chromium, SSR, and unsupported-browser boundaries.
- Updated the existing verification workflow to Node 24/action v6, explicit Chromium installation, read-only contents permission, and the package full gate.
- Added a manual, `main`-only `publish.yml` that uses the GitHub `npm` environment, OIDC, exact version validation, existing-package validation, and stage-only publication.
- Corrected the consumer smoke test to execute a real ESM consumer instead of resolving import-only package exports through CommonJS.
- Documented the one-time interactive npm bootstrap required before trusted or staged publishing can be registered.

### Changed Files

- `.github/workflows/publish.yml`
- `.github/workflows/verify.yml`
- `AGENTS.md`
- `README.md`
- `SECURITY.md`
- `package.json`
- `package-lock.json`
- `scripts/consumer-smoke.mjs`
- `reports/2026-07-15.md`

### Verification

- PASS: workflow YAML parse with Ruby Psych.
- PASS: `npm run verify:full` (`13` Vitest files, `105` tests, production build, `81` Playwright tests).
- PASS: `npm run test:run -- test/user-docs.test.ts` (`6` tests).
- PASS: `npm run test:consumer` in an unrestricted network environment; temporary ESM consumer installed `7` packages and imported all public JavaScript entries plus the stylesheet export.
- PASS: `npm --cache /tmp/comins-table-npm-cache pack --dry-run --json` (`25` package files, including `LICENSE`, `README.md`, `CHANGELOG.md`, JavaScript, types, and CSS).
- PASS: `npm view comins-table ...` returned registry `E404`, confirming that the bootstrap publication has not occurred.
- PASS: `git diff --check`.

### Residual Risks

- The first npm publication still requires an explicit maintainer action with npm 2FA; it was not performed.
- After bootstrap, npm must register `kim1124/comins-table`, `publish.yml`, and the `npm` environment as the trusted publisher with only `npm stage publish` allowed.
- GitHub dependency alerts, secret scanning, push protection, and `npm` environment reviewers remain remote settings that were not readable through the available GitHub connector and were not changed.
- Firefox and Safari remain outside the supported browser contract until Firefox and WebKit Playwright projects are added.

## Release Bootstrap Completion

### Work Time

- `2026-07-15 18:04 KST`

### Summary

- Published `comins-table@0.1.0` to the public npm registry with maintainer WebAuthn approval.
- Registered `kim1124/comins-table`, `publish.yml`, and the GitHub `npm` environment as the trusted publisher with only `npm stage publish` permission.
- Set package publishing access to require 2FA and disallow traditional publishing tokens.
- Enabled GitHub dependency alerts and Dependabot security updates; confirmed secret scanning and push protection are enabled.
- Created the GitHub `npm` environment with `kim1124` as the required reviewer and `main` as the only deployment branch.

### Changed Files

- `reports/2026-07-15.md`

### Verification

- PASS: `npm view comins-table@0.1.0 ...` returned version `0.1.0`, the expected repository, and the published integrity digest.
- PASS: registry installation added `5` packages and imported the root, core, clipboard, selection, and stylesheet exports.
- PASS: `npm trust list comins-table` returned repository `kim1124/comins-table`, workflow `publish.yml`, environment `npm`, and `stage publish` permission only.
- PASS: `npm access set mfa=publish comins-table` completed with WebAuthn approval.
- PASS: GitHub Verify completed successfully for commit `b00db50`.

### Residual Risks

- The bootstrap publication was interactive and therefore has no OIDC provenance; future staged releases use trusted publishing and receive provenance automatically.
- The GitHub environment permits self-review because `kim1124` is currently the only administrator; this is a manual approval gate, not separation of duties.
- GitHub repository rulesets remain unconfigured and were outside this release-bootstrap scope.
- Firefox and Safari remain outside the supported browser contract until Firefox and WebKit Playwright projects are added.
- The default npm cache still contains root-owned files; release verification used isolated temporary caches without changing home-directory ownership.
