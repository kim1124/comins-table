# 2026-08-31 Comins Table 0.1.9 구현

## 작업 요약

- 디자인 언어, CSS token 안정성, 컴포넌트화 경계를 공개 문서와 Canonical Feature Manifest로 정리했다.
- 공개 export, Playground route, 영문·한글 가이드, test evidence, TypeScript 문서 예제와 구조화된 제한을 자동 대조하는 문서 계약을 추가했다.
- Header Group 순서 복원, Row Group sticky content, Ctrl/Cmd 비연속 Cell 선택과 Row Drag lifecycle callback을 구현했다.
- Header Group/Rows 예제를 controlled data에 연결하고 toggle 대비와 모든 Detail 접기 회귀 검증을 보완했다.
- 이벤트·메서드 관련 Playground 페이지의 설명 영역에 이름, 설명과 사용 방법 표를 추가했다.

## 주요 변경 파일

- `DESIGN.md`, `docs/design/componentization.md`, `docs/feature-manifest.json`
- `src/core.ts`, `src/index.tsx`, `src/selection.ts`, `styles.css`
- `example/src/features/ColumnGroupFeature.tsx`, `example/src/features/RowFeature.tsx`, `example/src/styles.css`
- `scripts/check-documentation-contract.mjs`, `scripts/verify-package-artifact.mjs`
- 영문·한글 기능 가이드, README, CHANGELOG, package/lockfile
- 관련 Vitest, typecheck fixture, Playwright spec

## 수행한 검증

- Header order, Row Drag lifecycle, Duplicate ID, selection focused Vitest: 통과
- Header, Rows, Row Grouping, Selection, Playground layout affected Chromium E2E: 38개 중 제품 검증 37개 통과 후 잘못된 절대 순서 가정을 수정했고 해당 focused 재검증 통과
- 문서 계약 checker: 25개 shipped feature, 203개 root export 기준 통과
- `npm run verify`: 보안 21개, 라이선스 27개, Vitest 403개, TypeScript와 production build 전체 통과
- `npm run verify:package-artifact`: `comins-table-0.1.9.tgz` 실제 산출물 계약 통과, 검증 후 임시 산출물 제거
- `npm run test:e2e -- --workers=1`: Chromium E2E 161개 전체 통과
- 이벤트·메서드 표 추가 후 TypeScript, 다국어·문서 focused test 56개와 관련 Playground Chromium E2E 35개 통과
- PR Chromium E2E는 162개 중 161개 통과 후, 문서 표로 예제 위치가 내려갔지만 화면 밖 리사이즈 좌표를 사용한 Column Pinning 테스트 1개가 실패한 것으로 분류
- 해당 리사이즈 핸들을 viewport 안으로 이동하도록 테스트 입력을 교정하고 실패 항목 1개 및 Column Pinning 전체 5개 재검증 통과
- `git diff --check`: 통과

## 잔여 이슈

- 성능 테스트는 scroll/virtualization algorithm을 변경하지 않은 sticky visual geometry 수정이므로 계획 범위에서 제외한다.
- Firefox, WebKit 및 실제 Edge 브라우저 검증은 0.1.9 범위에 포함하지 않았다.
- PR #60은 검증 중이며 main 병합, publish, tag와 Release는 수행하지 않았다.

## Contract v1.8 공통 지침 적용

### 작업 요약

- Comins Contract v1.8 관리 블록을 적용했다.
- 제품 동작과 모듈 전용 지침은 변경하지 않고 민감정보 정책 테스트의
  Contract 버전 기대값만 갱신했다.

### 수행한 검증

- 영향받은 Contract 버전 테스트 1개 통과
- Codex 관리 설정 파싱 통과
- 지침 인벤토리 `findings: 0`
- `git diff --check` 통과

### 잔여 사항

- 이 기록 작성 시점에는 원격 Push, PR 및 병합을 수행하지 않았다.
